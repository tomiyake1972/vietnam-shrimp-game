# SAI-5 Fable事後監査報告（§15）

対象ブランチ: `feature/v2-sai5-market-evolution`（分岐元 `2195ae7` = SAI-4完了時点、監査時HEAD `3c94627`）
実施: 2系統の独立監査エージェントを並列実行（それぞれOpus・高推論、READ-ONLY）。
- 監査A: 二重計上・単位・時間順序・情報リーク・契約単価遡及・決定論（§15の1〜7）
- 監査B: 状態の二重効果・設備完成前能力・capex会計整合・異質preset会計・persistence後方互換・reasonと計算の一致（§15の8〜17）

両監査とも、コード精読に加えて`npx tsx`による**実行検証**（一時プローブは削除済み、リポジトリは無変更）を行っている。
指摘のうち主要なものは、報告受領後にClaude自身が該当コードを再確認し、妥当性を判定した（後述「Claudeによる採否判定」）。

---

## 0. 総括

**新規テスト64件・全1918件が成功しているにもかかわらず、SAI-5DとSAI-5Eの中核因果が意図どおり動いていない**ことが判明した。
いずれもテストが「状態が蓄積されること」「値が範囲内であること」しか検証しておらず、
**「その状態が実際に結果を変えること」を検証していなかった**ために素通りしていた。

| # | 分類 | 概要 | 位置 |
|---|---|---|---|
| A | **Blocker** | 成約配分が営業基盤の寄与を合計に入れていない → SAI-5Dは成約に無効、かつ0.08が消滅し外部オプションへ需要流出 | `sales/allocation.ts:216-221` |
| B | **Blocker** | 供給圧力の分子（5社のみ）と分母（ベトナム全体需要）のスケール不一致 → PD圧力≈0.28で倍率が上限へ片道上昇、設計と逆方向 | `companyLab/runner.ts:987-999` |
| C | Important | `realizedRatio`（品質・稼働率込み）と`referencePremiumRatios`（ベース比率のみ）の定義非対称 → 恒常的な割高バイアス | `marketEvolution.ts:132-137` |
| D | Important | 営業基盤が全社上限へ飽和（成約強化項にヘッドルーム欠如）→ SAI-5Bの初期差がT16頃に消滅 | `salesBase.ts:194` |
| E | Important | `config.sai5`がpersistence検証で脱落 → セーブ/ロードでSAI-5が全無効化、状態だけ凍結して残る | `persistence/schema.ts:864-876` |
| F | Important | `buildPublicMarketInfo`が`history.length`をturnの代理に使用 → 永続化経路でトレンドが恒久的にゼロ | `runner.ts:435-446` |
| G | Important | `SALES_BASE_ADVANTAGE`がウェイト0の構成でも「第6項として反映される」と主張（実測16回） | `runner.ts:421` / `heterogeneousPreset.ts:111` |
| H | Important | `PD_CAPACITY_MAINTAINED`が3箇所に定義されているのに発行箇所ゼロ | `reasonCodes.ts`ほか（発行元なし） |
| I | Important | `salesBaseScore`の注入がStandard AI経路のみ（autoPolicy・プレイヤー入力は中立50固定） | `autoPolicy.ts:374-376` |
| J | Important | 重大品質事故が営業基盤・customerTrust・qualityScoreの三重計上（モジュール自身の設計原則に反する） | `salesBase.ts:205-207` ↔ `quality/trustObservation.ts:72` |
| K | Calibration | SAI-5F capex拡張が代表シナリオで完全不活性（`noExcess`が常にfalse）→ 発火0回 | `decision/capex.ts:76,150` |
| L | Calibration | 供給ドライバが3経路（能力→稼働率／供給圧力→倍率／実現比率→普及）で重複作用 | 設計横断 |
| M | Calibration | 異質presetの固定資産調整が期初自己資本を最大4.1M USDずらす／傾斜が共通工程に阻まれ実効差が小さい | `heterogeneousPreset.ts:86-87,133` |
| N | Future | 主工場の選び方がAI観測（factoryIdソート順）とエンジン（配列出現順）で異なる（複数工場化で顕在化） | `observation.ts:83` ↔ `capex/capacityEffect.ts:176` |
| O | Future | 異質presetの「傾斜前fixture逆算」方式（現行値では誤差0だが基礎値変更で破綻） | `heterogeneousPreset.ts:120-135` |

**明確にOKと確認された項目**（監査が検証し問題なしと判定）:
需要の二重計上なし（総需要保存を数式展開と実測の両方で確認）／単位系の一貫性（HOSO換算トン・比率の混同なし）／
当期情報の先読みリークなし（インメモリ経路）／契約単価の遡及変更経路なし／決定論・再現性／
ウェイト合計1.0の検算／既定パラメータでのビット単位後方互換／設備完成前能力の観測非混入／
capex会計経路の非迂回・resumeRequests実処理経路の存在・借入承認前提の不在／
異質presetのBSバランス（丸め誤差0を数値で確認）／persistence v1〜v3受理・キー非包含・復元の決定論性／
reason code 4箇所同期（30件、欠落0）。

---

## 1. Blocker A: 成約配分が営業基盤の寄与を落としている

### 事実（Claudeが再確認済み）

`app/lib/v2/sales/allocation.ts:215-221`（エンジンが実際に通る`allocateMarketProduct`内）:

```ts
const breakdown = computeCompetitivenessBreakdown(entry, askPrice, basePrice, coverage, params);
const weight =
  breakdown.priceContribution +
  breakdown.coverageContribution +
  breakdown.relationshipContribution +
  breakdown.qualityContribution +
  breakdown.deliveryReliabilityContribution;   // ← salesBaseContribution が無い
```

SAI-5Dでは、公開ヘルパー`computeCompetitivenessWeight`（同`104-112`）にだけ第6項を追加したが、
**エンジンはこのヘルパーを呼ばず自前でインライン合計している**。
`grep`で確認したところ、`computeCompetitivenessWeight`の呼び出しは**テストファイルのみ**（16箇所すべて`__tests__`配下）。

### 影響（監査Aによる実測、5社・salesBaseScore=100/75/50/25/0・targetDemand=2000）

```
V1(weight 0)       | A=361.1 B=361.1 C=361.1 D=361.1 E=361.1 | 5社計 1805.4 | 外部 194.6
SAI5(weight 0.08)  | A=357.7 B=357.7 C=357.7 D=357.7 E=357.7 | 5社計 1788.7 | 外部 211.3
```

1. **営業基盤100の会社と0の会社の成約量が完全に同一** → SAI-5Dは成約に対して完全に無効。
   蓄積・観測・診断（`SALES_BASE_ADVANTAGE`）・CSVトレースだけが動いている。
2. `SALES_PARAMETERS_SAI5_SALES_BASE_V1`はcoverage/relationship/qualityから計0.08を切り出して`salesBase`へ移したが、
   その0.08が**どこにも足されず消滅**する。5社の競争力ウェイト合計が0.589→0.542（−8.0%）となり、
   固定ウェイト0.35の外部オプションへ需要が流出（5社計−0.93%、外部+8.6%）。
   `sales/parameters.ts:148-152`の「合計を1.0に保つのは外部オプションとの相対バランスを変えないため」という
   設計意図が**実装で守られていない**。

### 推奨修正

`allocateMarketProduct`の`weight`に`breakdown.salesBaseContribution`を加算する
（または`computeCompetitivenessWeight(...)`を呼ぶ形へ一本化する）。
併せて「営業基盤が高い会社の成約量が実際に多くなる」ことを検証するテストを追加する
（現行のSAI-5D統合テストは状態の蓄積しか見ておらず、この欠落を素通りした）。

---

## 2. Blocker B: 供給圧力の分子・分母のスケール不一致

### 事実（Claudeが自身のA/B実測データで確認済み）

`companyLab/runner.ts:987-999`:
- **分子** = `decisions[].salesPlans[].desiredQuantity` の合計 = **ラボ5社のみ**の希望販売量
- **分母** = `turnResult.salesRecord.allocations[].targetDemand` の合計 = **ベトナム産全体**がその市場×商品で
  獲得する需要（`hosoPrices.VN.allocatedDemand`由来＝国家規模）

加えて`maximumSupplierShare=0.35`・`externalOptionWeight=0.35`により、
5社が`targetDemand`を埋め切る構造にそもそもなっていない。

### 実測（`artifacts/sai5/ab-comparison/summary.json`、median、turn1→8）

| config | 指標 | t1 | t2 | t3 | t4 | t5 | t6 | t7 | t8 |
|---|---|---|---|---|---|---|---|---|---|
| feedbackOnly | 供給圧力PD | 0.290 | 0.223 | 0.236 | 0.226 | 0.222 | 0.191 | 0.254 | 0.240 |
| feedbackOnly | プレミアム倍率PD | 1.000 | 1.120 | 1.241 | 1.297 | 1.334 | 1.356 | 1.375 | 1.375 |
| allOn8q | 供給圧力VAP | 1.661 | 1.008 | 0.994 | 0.890 | 0.945 | 0.811 | 0.740 | 0.679 |
| allOn8q | プレミアム倍率VAP | 1.000 | 0.880 | 0.919 | 0.952 | 0.993 | 1.009 | 1.041 | 1.076 |

PDの供給圧力は**構造的に0.2〜0.3**（＝需要の3〜5倍の余地がある状態）。
目標倍率 = `1 − (0.28−1)×0.5 = 1.36` となり、**上限1.4へ向かう片道ラチェット**になる。
VAPは初期のみ1を超えるため一度は低下（0.88）するが、圧力が1を下回った後は同様に上昇へ転じる。

つまり「5社が供給を増やすと翌期プレミアムが下がる」という設計意図に対し、
実装は**恒常的にプレミアムを押し上げるだけの定数バイアス**として作用している
（監査Aの独立実測では、フィードバックON/OFFでPDプレミアムが16Q時点で+28.4%）。

### 派生影響

`capexOversupplyPressureThreshold=1.15`・`supplyPressureRetreatThreshold=1.2`は
実測圧力0.2〜0.5に対して**永久に発火しない**。SAI-5Fの「過剰供給リトリート」
（`decision/capex.ts:76-96`、`decision/sales.ts:190-208`）は実質デッドコードであり、
`SUPPLY_PRESSURE_RETREAT`・`CAPEX_DEFERRED_OVERSUPPLY`・`VAP_OVERSUPPLY_RETREAT`の
発火回数が代表16Q実行で**すべて0回**である事実と整合する。

### 推奨修正（いずれか）

1. 分母を「5社に帰属する需要」に揃える（`targetDemand ×（1 − externalOptionの期待取り分）`等）
2. 分子を「ベトナム全体の供給」に揃える
3. 圧力を自身の初期ベースラインで正規化する（`pressure / pressure_baseline`）

修正後は`capexOversupplyPressureThreshold`等のしきい値も再校正が必要。

---

## 3. その他のImportant（要点のみ。詳細は上表の位置参照）

- **C** `realizedRatio = premium/hosoPrice` には`utilizationMultiplier`と品質プレミアム（全国qualityScore=60により恒常+0.03）が
  含まれるのに、比較対象の`referencePremiumRatios`はベース比率のみ。需給が完全中立でも「PDは16.7%割高」と誤判定され、
  PD普及が恒久的に約1.33四半期後ろ倒しされる。さらにBのラチェットが「PDが割高になった」と誤読され自己増幅する。
- **D** `salesBase.ts:194`の成約強化項に`(1 − score/100)`のヘッドルーム係数が無いため、活動中セルの不動点がcap=100になる。
  20Q実測で全社平均が97〜99へ飽和し、SAI-5Bが仕込んだ初期差（55〜65）はT16頃に完全に洗い流される。
- **E** `persistence/schema.ts:864-876`の`validateCompanyLabConfig`が`{scenarioId, mode, seed, turns}`のみを再構築するため
  `config.sai5`が脱落。状態（`salesBaseState`/`marketEvolutionState`）はv4で往復するのに、それを意味づけるフラグだけが
  失われ、セーブ/ロード後は「機能OFFだが状態は凍結して残る」不整合になる。現時点ではApplication Service層が
  `sai5`を設定していないため未顕在だが、UI配線と同時に必須。
- **F** `runner.ts:435`の`nextTurn = state.history.length + 1`は、永続化経路
  （`companyLabQuarterFlowService.ts:374`が意図的に直近1件のみ注入）では常に1になり、`nextTurn >= 3`が永久にfalse。
  結果`quarterlyTrendByMarket`が恒久ゼロとなり、SAI-5Fの成長エントリ・販売前傾が実ゲームでは一切発火しない。
  `state.scenarioState.currentTurn`基準へ変更すべき。
- **G/H/I/J** は上表のとおり。特にHは「PD能力維持の判断」自体が未実装であり、
  SAI-5E（PD⇔VAP代替）に対するAI側の片側だけが接続されている状態。

---

## 4. Claudeによる採否判定

監査指摘を無条件には採用せず、コードと仕様に照らして以下のとおり判定した。

**採用（実コードで再確認済み）**: A（`allocation.ts:215-221`のインライン合計に第6項が無いこと、
`computeCompetitivenessWeight`の呼び出しがテストのみであることを`grep`で確認）、
B（自身のA/B実測データで供給圧力0.2〜0.3と倍率の単調上昇を確認）。

**採用（監査の実測を妥当と判断）**: C・D・E・F・G・H・I・J。
いずれも指摘位置のコードが実在し、論理が追える。

**Calibrationとして記録（今回は係数調整せず）**: K・L・M。
§14「今回の目的は最終ゲームバランスの完成ではない」に従い、A・Bの修正後に再測定してから扱う。
特にLは、A・Bを直さないまま係数を触ると誤った方向へ校正してしまうため、**修正前の調整は行わない**。

**Future（次工程）**: N・O。現行の全社1工場・現行テンプレート値では顕在化しないことを確認済み。

**不採用**: なし（明確に誤りと判断できる指摘は無かった）。

なお監査Aは、既存テスト`sai5MarketEvolution.test.ts`の「契約単価は遡及変更されない」テストが
同一配列同士の比較で**恒真**になっていると指摘した。契約単価の不変性自体はコード読解で確認済み
（`sales/contracts.ts:88`でスナップショット、変更経路なし）だが、テストとしては保証になっていないため、
修正時に併せて実効的な検証へ差し替える。

---

## 5. テスト網羅の穴（今回の学び）

本監査で最も重要な発見は、個々の不具合そのものよりも**テスト設計の穴**である。

| 穴 | 素通りしたBlocker |
|---|---|
| SAI-5D統合テストが「状態が蓄積される」「観測できる」しか検証せず、**成約配分を変えることを一度も検証していない** | A |
| SAI-5E統合テストが「有限であること」「0.6〜1.4に収まること」のみで、**倍率が意図した向きに動くことを検証していない** | B |
| 永続化往復テストに`config.sai5`の往復ケースが無い | E |
| 代表シナリオでの各reason codeの**発火実績**を検証していない | H・K |

修正時には、各機能について「状態が変わる」ではなく
**「その状態が実際に結果（成約量・価格・投資判断）を変える」**ことを検証するテストを追加する。

---

## 6. 修正優先順位（推奨）

1. **A**（`allocation.ts`に`salesBaseContribution`を加算）— 影響が明確。併せて成約差の実効テストを追加。
2. **B**（供給圧力の分子・分母のスケール整合）— 係数調整では直らない定義バグ。修正後にしきい値を再校正。
3. **C**（`referencePremiumRatios`側に稼働率倍率・品質加算を含める、または`realizedRatio`から品質加算を除く）
4. **D**（成約強化項にヘッドルーム係数を掛ける）、**G**（観測注入と初期営業基盤をフラグでゲート）
5. **E**（`validateCompanyLabConfig`に`sai5`のoptional検証を追加）、**F**（`scenarioState.currentTurn`基準へ）
6. **H**（`PD_CAPACITY_MAINTAINED`の判断を実装するか、コード定義を削除して整合させる）、**I**、**J**
7. **K・L・M**（A〜Cの修正後に再測定してから校正）

**重要**: A・Bの修正はA/B比較の全数値を変える。`scripts/sai5Analysis.ts`による再実行と、
本ブランチの完了報告書の数値差し替えが必要になる。

---

# 第2部: 修正と Fable 再監査（2026-07-31）

三宅さんのご指示により、上記の指摘をすべて修正し、**変更箇所と因果経路に絞った
Fable再監査**（Opus 5・reasoning high・2エージェント並列、視点を分けて実施）を
行った。以下は各指摘の最終ステータスである。

## 7. 指摘ごとの最終ステータス

凡例: **修正済み** / **Calibrationとして残す** / **Futureとして残す** / **不採用（理由付き）**

| ID | 指摘 | ステータス | 対応内容・判断理由 |
|---|---|---|---|
| **A** | 成約配分が営業基盤の寄与を落としている（Blocker） | **修正済み** | 合計処理を `sumCompetitivenessContributions` へ一本化。係数キー→内訳キーの対応表をマップ型で定義し、係数を1つ足すと合計にも入れるまでコンパイルが通らない構造にした（再監査で `tsc` により実際に確認済み）。加算順は旧手書きと同一のため、ウェイト0の既定パラメータではビット単位で不変。 |
| **B** | 供給圧力の分子・分母のスケール不一致（Blocker） | **修正済み** | 候補4種を実装して 4seed×32Q で実測比較し、中立状態で1.0を中心にできた `completed_supply`（= 1 + 売れ残り/対象需要）を採用。分子は営業工数制約**適用後**の提示量に修正。採否理由は設計書 §2.3-R に全候補の実測値つきで記録。 |
| **C** | 実現/参照プレミアム比率の定義不整合 | **修正済み** | 両辺を「品質プレミアムを除いたベースプレミアム比率」にそろえた（`realizedBasePremiumRatio`）。PD⇔VAP代替の判定も同じ分解に統一。再監査で分解が厳密に正しいこと（`realizedBase == 基準比率 × 稼働率倍率 × SAI-5E倍率` が全24Q一致）を実測確認。 |
| **D** | 成約強化にヘッドルームが無く上限へ早期飽和 | **修正済み** | 活動獲得と同じく `(1 - score/100)` を乗じる。あわせて成約充足率の分母も営業工数制約適用後へそろえた（再監査の追加指摘）。順位が初期値で固定されないことを「撤退した先行を継続営業した後発が追い越す」テストで検証。 |
| **E** | 永続化で `config.sai5` が失われる | **修正済み** | `validateSai5FeatureFlags` を追加。未知の `supplyPressureDefinition` は検証エラー、旧スキーマ（キーなし）は undefined ＝従来挙動（マイグレーション不要）。 |
| **F** | turn を `history.length` から導出しており永続化経路で破綻 | **修正済み** | `scenarioState.currentTurn`（正典）へ変更。さらに再監査の過程で「前々期の適用構成比が履歴配列の深さに依存する」ことが判明したため、carry state に `recentAppliedMixes`（直近2件）を持たせ、直近1件だけを注入する復元経路でも中断なし実行と完全一致することを検証した。 |
| **G** | `SALES_BASE_ADVANTAGE` が発火条件を満たさずに出る | **修正済み** | エンジンが当期に実際に使うウェイトを観測へ公開し（`salesBaseCompetitivenessWeight`）、正のときだけ発火。再監査の再指摘（「機能OFFでは元々発火しない＝テストが無効」）を受け、修正が効く唯一の状況「基盤は観測できるがウェイト0」を直接検証するテストを追加。 |
| **H** | `PD_CAPACITY_MAINTAINED` が一度も発火しない | **修正済み** | 実際の判断へ接続。当初「PDの単価採算がVAPより良い」を条件にしたが、本モデルのVAPは基準比率が構造的に高く（0.55 vs 0.18）PDが上回る局面は存在しない＝定義上一度も発火しない条件だったため、「VAPが過熱している局面で、投資済みで自力採算の取れているPDを回し続ける」という限界的判断へ改めた。代表シナリオ（4seed×32Q）で82回、8Q・12seedで248回発火。 |
| **I** | autoPolicy・手入力経路が営業基盤を渡していない | **修正済み** | runner 側で正典の `SalesBaseState` から一括上書き（`applyAuthoritativeSalesBaseScores`）。意思決定側の自己申告値は採用されない（機能ONなら1期目でも削除する）。機能OFF・エントリ無しではキー自体を付けない（未取得値を50で埋めない）。 |
| **J** | 品質事故の3経路が過大かもしれない | **修正済み（削除はしない）** | severity 0.5 の事故1件の伝播を数値測定。3経路は粒度・観測系列・回復速度が異なり三重計上ではないことを記録。合計影響は合成競争力の約2%で過大ではないが、営業基盤経路が48%と主シグナルの品質(39%)を上回っており、かつ営業基盤だけが severity を無視した定額だった。**severity 比例へ統一**して寄与26%に収め、毀損自体は残した。同一四半期の複数バッチは最大severity1件ぶんに集約。 |
| **K** | `noExcess` が常に false で SAI-5F 拡張が発火しない | **修正済み** | Bの構造修正後にしきい値を実測から測り直した結果、`SUPPLY_PRESSURE_RETREAT` 420件 / `VAP_OVERSUPPLY_RETREAT` 60件 / `LIFECYCLE_GROWTH_PURSUED` 660件（allOn8q）が発火するようになった。 |
| **L** | しきい値 1.15 / 1.2 が旧定義前提で到達不能 | **修正済み** | 供給圧力EWMAの実測分布（PD 1.000〜1.044 / VAP 1.070〜1.318）に基づき `supplyPressureRetreatThreshold` 1.2→1.14、`capexOversupplyPressureThreshold` 1.15→1.20 へ。 |
| **M** | ライフサイクル有効時の default 率が高い | **Calibrationとして残す** | 構造バグではないことを数値で確定させた。ライフサイクルON/OFFで**対象需要の合計が完全に一致**する（8四半期すべてで差 0.000000）ため二重計上・需要の増減はない。需要が低採算のHOSOへ寄る一方、5社の設備構成が現状のライフサイクル曲線に合っていないことによる採算悪化であり、設備構成・ライフサイクル係数の校正課題。 |
| **N** | 32Qで倍率が上下限へ張り付く懸念 | **修正済み（解消を確認）** | 修正後の実測では床(0.6) 0.0% / 天井(1.4) 0.0%（allOn32q 256サンプル、allOn8q 192サンプル）。VAP倍率は 1.000→0.843→0.958 と底を打って回復し、一方向の張り付きはない。 |
| **O** | 5社の挙動が同質化する懸念 | **一部修正済み／一部 Calibration** | 志向・経営性格の適用で分岐開始turnは1（全機能ON）。ただし営業基盤は32Qで全社90前後に収束し会社差が0.18ポイントまで縮む → **Calibrationとして残す**（後述 R-4）。 |

## 8. 再監査で新たに出た指摘とその扱い

再監査は2つの視点で行った。(1) 実装の妥当性、(2) 追加したテストの実効性。

### 8.1 実装側 — 新規欠陥は検出されず

再監査の結論は「HEADのコード自体は A〜J すべてについて指摘どおり直っており、
**新規の二重計上・情報リーク・非決定性・後方互換の破壊は検出できなかった**」。
根拠として次が実測で確認された。

- 供給圧力の残差恒等式 `(提示量+外部量) = 対象需要` が実運転で成立（T1 PD: 15141.5 + 23429.2 = 38570.7）
- 最大供給者シェア上限（対象需要×0.35）は 24Q×1,200セルで一度も拘束せず、恒等式を壊さない
- 内訳6項目の合計と配分に使われたウェイトの差が 1,200セルすべてで厳密に 0
- 時間順序: プレミアム倍率＝前期末state由来、PD⇔VAP代替＝前期marketResult、carry state更新＝当期末。遡及なし
- 情報境界: `productSupplyPressureOutlook` は前期末の集計値、`salesBaseByMarketProduct` は自社分のみ、`salesBaseCompetitivenessWeight` は全社共通のエンジン係数
- 決定論: 同一入力を2回実行して全出力が完全一致
- 後方互換: 機能OFFで既存挙動と一致（唯一の差は `CompanyOwnState` に `salesBaseCompetitivenessWeight: 0` が付く形状のみで、数値結果に影響なし）

### 8.2 テスト側 — 実効性の問題を4件検出、すべて修正済み

再監査は10種類のリグレッションを実際に実装へ注入して検出力を測定した。
そのうち**2件が「1971テスト中0件しか落ちない」**ことが判明した。

| 注入したリグレッション | 修正前の検出 | 修正後の検出 |
|---|---:|---:|
| Blocker A 再現（合計から salesBase を除く） | 因果テストは素通し（5件） | **6件**（因果(1)を含む） |
| Blocker B 再現（定義を旧実装へ戻す） | 9件 | 9件 |
| 指摘D 再現（ヘッドルーム除去） | 1件 | 1件 |
| 指摘E 再現（sai5復元を外す） | 3件 | 3件 |
| 指摘I 再現（正典上書きを素通し） | 1件 | 1件 |
| **runner が config.sai5.supplyPressureDefinition を無視** | **0件** | **1件** |
| 指摘B（分子が営業工数スケールを無視） | 1件 | 1件 |
| 指摘F（recentAppliedMixes を素通し） | 1件 | 1件 |
| **指摘G のウェイト条件を外す** | **0件** | **1件** |
| 指摘F（turn を history.length へ戻す） | 1件 | 1件 |

修正内容:

- **因果(1) が Blocker A を検出できなかった（Important・修正済み）** — 機能ON/OFF比較は
  salesBase 以外に coverage/relationship/quality の3ウェイトも同時に変わるため、
  salesBase 寄与がゼロでも「結果が変わった」が成立していた（交絡）。1回のON実行の中で
  「実配分に使われた `competitivenessWeight` が内訳6項目の合計と一致するか」を見る形へ置換。
  **内訳の合計はテスト側で手書きし、実装の `sumCompetitivenessContributions` を使わない**
  （同じ関数を両辺で使うと、実装から項を落としたとき両辺が同時に変わって検出できない）。
- **`config.sai5.supplyPressureDefinition` の実行時経路が無テスト（Important・修正済み）** —
  永続化のラウンドトリップはフラグの往復しか保証しておらず、「復元した定義がエンジンで
  実際に使われる」ことが未検証だった（Blocker B と同型の穴）。因果(7) を新設。
- **指摘G のテストが前提不成立（Important・修正済み）** — 機能OFFではそもそも
  `salesBaseByMarketProduct` が観測に載らないため、ガードの有無に関係なく発火しなかった。
  修正が効く唯一の状況「基盤は観測できるがウェイト0」を直接検証するテストへ置換。
- **トートロジー・恒真assert 6件（修正済み）** — `max - min >= 0`、`f(g(x)) === f(g(x))`、
  同一引数での2回呼び出し比較、常に1.0になる値どうしの比較、実行されない guard 内の assert、
  集めた変数を参照しないテスト、デッドコード。すべて意味のある検証へ置換または削除。
- **検出力の弱かったテスト3件（修正済み）** — 指摘Dの順位入替テストが成約なしで回っており
  ヘッドルームの対象経路を通っていなかった／指摘Iのテストが「偽装が通らない」片側のみ／
  品質事故の伝播が合成競争力までで止まり成約量に届いていなかった。

### 8.3 Calibration として残すもの（構造は正しいが校正が必要）

| ID | 内容 | 実測 | 判断 |
|---|---|---|---|
| **R-1** | **供給圧力のスケールが商品・時間で不変でない** — 分母が「全ベトナム対象需要」のため、中心は1.0にそろうが振れ幅がそろわない。PD は 1.000〜1.048、VAP は 1.060〜1.796。またベトナム総需要が32Qで約2倍になるため、同じ売れ残りトン数が後期ほど小さい圧力として読まれる。 | しきい値1.14/1.20 が事実上VAP専用。VAPのEWMAも 1.32→1.08 へ低下（うち一部は分母の膨張が原因） | **Calibrationとして残す**。再監査は分母を「5社の提示量」に取る（＝売れ残り率）案を提示しており、採用条件（当てはめ定数なし・1.0中心・方向性）をすべて満たしつつスケール不変になる。次回の校正フェーズの第一候補として設計書へ記録。今回は構造的因果が確認できているため変更しない。 |
| **R-2** | **フィードバックが片側のみ** — `completed_supply` は構造上 pressure ≥ 1 のため、プレミアム倍率は永久に1.0以下。`premiumMultiplierCap = 1.4` は到達不能。 | PD 0.978〜1.000 / VAP 0.841〜1.000 | **意図的な制約として残す**。本モデルの外部選択肢は完全弾力的で、市場が構造的に供給不足になることが起こり得ない。対称な上振れを作るには根拠のない基準定数が必要で、ご指示の禁止事項に触れる。プレミアムの上方向は既存の世界需給・稼働率経路が担当する。設計書 §2.3-R に明記済み。 |
| **R-3** | **割安シグナルの支配項が世界稼働率** — 稼働率倍率を意図的に残しているが、残るバイアスは指摘Cで除いた品質プレミアムより一桁大きい。T1 VAP の割安シグナル +0.46 は100%が世界VAP加工能力の余剰に由来（SAI-5E倍率の寄与はゼロ）。 | cheapness +0.46（T1）→ +0.10（T12）→ −0.12（T24）と収束的に振動。発散しない | **Calibrationとして残す**。「5社の供給行動→普及速度」という設計上の因果が、実質的に外生のシナリオ能力トレンドに覆われている。参照側にも同じ稼働率倍率を掛けて「5社起因の割安分だけ」を抽出する案を設計書へ記録。 |
| **R-4** | **営業基盤が32Qで全社90前後へ収束** — ヘッドルーム導入で上限100への飽和は防げたが、全社が全市場×全商品で毎期営業するため均衡値が高止まりし、会社差が0.18ポイントまで縮む。 | セル単位では最大3.1ポイントの差が残り、方向は志向どおり（JP×VAPはJPQが最高、US×PD/VAPはVAP社が最高、EU×PDはCONSVが最高） | **Calibrationとして残す**。差の**向き**は正しく、機構は機能している。差の**大きさ**を出すには獲得速度を下げるか減衰を強める必要がある。 |
| **M** | **ライフサイクル有効時の default 率**（97.5%、control 42.5%） | 対象需要の合計はON/OFFで完全一致（8四半期すべて差0） | **Calibrationとして残す**（上表参照）。 |

### 8.4 Future として残すもの

| ID | 内容 | 判断 |
|---|---|---|
| **F-1** | 本修正より前に保存されたスナップショット（`recentAppliedMixes` なし）は、復元後2四半期のあいだ従来の履歴フォールバックで動く。 | **Futureとして残す**。害は小さいが、移行期の挙動としてリリースノートに記載する。 |
| **F-2** | 供給圧力の分子が「営業工数制約適用後」ではあるが、処理能力上限・最大供給者シェア上限で切られる前の値である。実運転ではシェア上限が一度も拘束していないため影響はないが、希望量が処理能力を大きく超える会社が現れると、供給し得ない量が売れ残りに計上され得る。 | **Futureとして残す**。R-1 の分母見直しと同時に整理するのが自然。 |
| **N-旧** | HOSO⇔VAP の直接代替（現在0で保守的に開始） | **Futureとして残す**（当初の設計判断どおり）。 |

### 8.5 不採用としたもの

| 提案 | 不採用の理由 |
|---|---|
| 「品質事故の3経路のうち営業基盤側を削除する」 | 3経路は粒度（会社×商品／会社×市場／会社×市場×商品）・観測系列（生産バッチ／納品体験／営業活動）・回復速度がすべて異なり、同じシグナルの三重計上ではない。合計影響も合成競争力の約2%で過大ではない。削除ではなく severity 比例化で主従を正した。 |
| 「PD_CAPACITY_MAINTAINED を削除する」 | 当初のご指示で必須とされた reason code であり、削除は指示に反する。実際の判断（VAP過熱局面でPD能力を維持する）へ接続した。 |
| 「`PD_CAPACITY_MAINTAINED` の条件に『PDの単価採算がVAPより良い』を含める」 | 本モデルのVAPは基準プレミアム比率が構造的にPDより高く（0.55 vs 0.18）、絶対額でも最低受注水準比でもPDが上回る局面は存在しない。条件に含めると定義上一度も発火せず、「実体のない reason code」へ逆戻りする。 |
| 「営業基盤の順位反転をテストの合格条件にする」 | 実測で反転が0件。営業基盤は成約実績で伸びるため他の競争力要素と正の相関を持ち、順位を覆すより既存の順位を強める方向に働く。これは蓄積モデルの性質であり実装の欠陥ではない。反転ではなく「差の大きさが競争力の最小差を超えるか」で判定する。 |
| 「代表シナリオで発火しない reason code のために本体の条件を緩める」 | ご指示§4の「不自然な本体ロジックを足してまで発火させない」に従い、専用fixtureでの経路健全性の確認に留めた（`CAPEX_DEFERRED_OVERSUPPLY` / `VAP_GROWTH_ENTRY` / `CAPEX_RESUME_PROPOSED`）。 |

## 9. 再監査の実施条件についての注記

2つの再監査エージェントを並列実行したため、テスト実効性側のエージェントが
リグレッションを注入している最中に、実装妥当性側のエージェントが同じ作業ツリーを
読み取り、一時的な改変を「混入」として検出する事象が起きた。**両エージェントの
終了後に `git diff` が空・`git status` がクリーンであることを確認済み**であり、
本報告は HEAD のコミット内容に対するものである。同種の並列監査を行う場合は、
作業ツリーを分けるか直列に実行することを推奨する。
