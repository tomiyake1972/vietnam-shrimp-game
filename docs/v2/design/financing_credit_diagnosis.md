# 資金繰り・与信診断 — 「通常融資の承認がゼロ」の原因究明

**文書状態**: 診断完了・**製品コードの変更なし**（追加したのは診断スクリプトと
characterizationテストのみ）。
**ブランチ**: `feature/v2-financing-credit-diagnosis`（`origin/develop/v2` @ `90d67bc` 起点）。
**再現手段**: `npx tsx scripts/financingCreditDiagnosis.ts [--variants|--params|--csv]`

---

## 1. 発端と、**前回報告の訂正**

Phase A の SAI-3A グリッド探索（`/tmp/pd_labor` の48コミットスタック）で、
14点すべてにおいて「通常審査を通った借入 = 0.00回」と観測し、
**「financing.ts の資金繰り判断が実質的に働いていない」と報告した。**

**この報告は誤りだった。** 本診断で、通常融資の承認経路は正常に動作することを確認した。
`origin/develop/v2` 上で同じ標準初期条件・同じシードを実行すると、
**120行中44行で通常融資が承認され、承認総額は 264,175,616 USD** である。

誤った結論に至った原因は2つあり、両方とも私の側にある。

### 1-1 グリッドの軸のラベルを取り違えていた（主因）

`GridPoint` の `shortTermLoansUsd` / `longTermLoansUsd` を、私はコメントで
「借入枠（資金繰り判断が働く余地を決める副軸）」と説明していた。
**実際にはこれは既存の借入残高である。**
`financing/initialPortfolio.ts` が `financeState.shortTermLoans` / `longTermLoans` を
1対1で `LoanRecord` へ写し、それが `borrowingCapacity` の `existingLoanBalance` になる。

```
追加借入可能額 = max(0, grossLimit − existingLoanBalance)
```

つまり私はグリッドで「余力を増やす軸」のつもりで**債務を増やしていた**。
P5/P6/P7 では債務を 95,000,000 USD（ST 40M + LT 55M）まで積み上げており、
担保ベース上限（約 37〜41M）を大きく上回るため、余力は構造的に恒久ゼロになる。

### 1-2 破綻していた点では凍結が重なっていた（副因）

債務を上げなかった点（P0〜P4, R1, R2, R4〜R6）は、48コミットスタックの
PD労働係数 1.8 により全社が支払不能に至っており、
`underwritingFrozen = creditTier === "E" || severeArrears || insolvent` で
通常融資が停止していた。これは**設計どおりの挙動**である。

### 1-3 ただし「P5は破綻率0%なのに承認0」は1-1だけで説明できる

`sai3a-grid-search.json` を読み直すと P5 は破綻率 **0%**、R3 は **5%**、P6 は **2%** で、
それでも通常融資 0.00 だった。これらはすべて債務を 84〜95M へ引き上げた点であり、
**1-1（債務過大）だけで完全に説明がつく**。凍結では説明できない。

**なお本ブランチには48コミットスタックが無いため、スタック側の数値そのものは
ここでは再現していない。** 上記は develop/v2 上での対照実験と、
スタックの成果物JSON（読み取りのみ）の突き合わせによる。

---

## 2. 通常融資の処理経路（実コード）

```
① standardAi/decision/finance.ts  buildStandardAiFinancingRequest
     desiredAmountUsd = cash < targetMinimumCash ? targetMinimumCash − cash : 0
     desiredLoanType = "workingCapital", 満期4Q, 満期一括, emergencyAcceptable = true
        ↓
② financing/liquidityClose.ts  planQuarterFinancing      ← 期首（前期末情報のみ）
     ├ computeCreditScore                → tier A〜E
     ├ checkCovenants                    → anyBreach
     └ computeBorrowingCapacity          → availableAdditionalCapacityUsd
        ↓
③ financing/bankUnderwriting.ts  underwriteLoanApplication
     frozen なら 承認0
     それ以外 approvedAmountUsd = min(desiredAmountUsd, availableAdditionalCapacityUsd)
        ↓
④ financing/liquidityClose.ts  closeQuarterWithFinancing
     normalDrawUsd = plan.underwriting.approvedAmountUsd
     → LoanRecord を生成（isEmergency: false）
     → Pass1 で loanDrawUsd として現金へ反映
        ↓
⑤ 緊急融資の判定（通常融資「後」の不足額に対してのみ）
     shortfall = max(0, 利息 + 約定元本 − max(0, Pass1後の現金))
     shortfall > 0 かつ emergencyAcceptable なら緊急融資
        ↓
⑥ totalLoanDrawUsd = normalDrawUsd + emergencyDrawUsd
   → Pass2 で確定、endingShortTermLoans / endingLongTermLoans へ反映
```

### 2-1 借入余力の式

```
collateralBasedLimit = 売掛金×0.7 + 使用可能原料×0.4 + 未着原料×0.15 + 製品×0.3
earningsBasedLimit   = max(0, 前期EBITDA相当【四半期値】) × 2.0
creditTierCap        = max(0, 純資産) × {A:1.5, B:1.1, C:0.75, D:0.4, E:0}

grossLimit           = min( max(collateralBasedLimit, earningsBasedLimit), creditTierCap )
availableAdditional  = frozen ? 0 : max(0, grossLimit − existingLoanBalance)
```

---

## 3. ゼロ承認の直接原因

**標準初期条件が、担保ベース上限（約37,000,000 USD）を大きく上回る
既存債務（57,000,000 USD = ST 24M + LT 33M）を最初から持っている。**

したがって turn1 の時点で `availableAdditionalCapacity = max(0, 37.17M − 57M) = 0`。
**会社は健全（信用区分B・スコア75.3・純資産100,327,709 USD）である。**
債務超過でも延滞でもなく、凍結もされていない。それでも承認額は0になる。

turn5 で初期短期借入（満期4四半期の満期一括）が満期到来して返済され、
既存債務が 57M → 27.28M へ落ちる。これで初めて余力が生まれ、**turn6 から承認が始まる**。

| turn | 申請件数 | 承認件数 | 凍結件数 | 緊急件数 | 承認総額 |
| --- | --- | --- | --- | --- | --- |
| 1 | 15 | **0** | 0 | 0 | 0 |
| 2 | 0 | 0 | 0 | 0 | 0 |
| 3 | 15 | **0** | 0 | 0 | 0 |
| 4 | 15 | **0** | 0 | 0 | 0 |
| 5 | 15 | **0** | 0 | 15 | 0 |
| 6 | 15 | **15** | 0 | 0 | 161,501,101 |
| 7 | 15 | 14 | 0 | 0 | 9,548,660 |
| 8 | 15 | 15 | 0 | 0 | 93,125,854 |

**凍結件数は全期間で0。**
【用語是正・§11-6参照】却下は全て、既存借入残高(`existingLoanBalance`)が
`grossLimit`を上回った（＝`availableAdditionalCapacityUsd = max(0, grossLimit -
existingLoanBalance)`がゼロになった）ことによる。この`grossLimit`の算定基準
として`collateralBasedLimit`が選ばれていた（`bindingConstraint = collateralBased`）
というのは「担保が絞っている」ことを意味しない——`max(collateral, earnings)`の
うち担保ベース値の方が大きかったというだけであり、余力をゼロにした真因は
既存債務控除の方である（詳細は§11-6・§12の用語是正を参照）。
信用区分は却下61件のうち A が31件・B が30件、
つまり**却下されたのはすべて信用区分AまたはBの健全企業**である。

---

## 4. バグか、仕様・パラメータの問題か

**バグではない。** 三宅さん指定のチェック項目を個別に確認した結果は以下のとおり。

| チェック項目 | 結果 |
| --- | --- |
| 単位の取り違え（百万 vs ドル） | なし。全経路USD。`creditTierCap` = 純資産100,327,709 × 1.1 = 110,360,480 で整合 |
| 四半期値と年換算の混同 | **コードは自己整合**。入力名 `ebitdaLikeQuarterlyUsd`、係数名 `earningsMultiple`。ただし「四半期EBITDA×2.0」は年間EBITDA換算で0.5倍にあたり、一般的な2〜3倍より保守的（§6候補1） |
| 符号反転 | なし |
| DSCR・自己資本比率・流動比率・負債比率の分子分母 | covenant側。本件では `anyBreach = false` が全期間continuedで、拘束していない |
| 借入前後の状態の取り違え | なし。`prevFinanceState`（前期末）を使用。銀行が当期実績を見ない設計はコメントでも明示 |
| 参照タイミング | 正しい。期首に確定し、当期のturnResultは受け取れない関数シグネチャ |
| 既存債務の二重控除 | grossLimitの算定基準として選ばれた経路（担保ベース）では**なし**。純資産経路（tierCap）は純資産が債務控除後の値なので二重性はあるが、tierCapがgrossLimitを実際に縮小した行は§13の集計で0件（develop/v2）〜15件（pd_labor）であり、大半の行では影響しない |
| undefined・初期値による自動却下 | turn1の `earningsBasedLimit = 0`（前期実績が無いため）は事実。ただし担保ベースが常に上回るため原因ではない |
| **健全な初期企業も却下されるか** | **却下される。これが本件の核心**（§3） |
| 承認されたのに実行されない経路 | **ない。** turn6: 承認 11,321,353 → `loanDrawUsd` 11,321,353 → 短期借入 2,530,739 → 13,852,092 と正しく反映 |
| 通常と緊急の二重実行 | なし。turn5 は 通常0 + 緊急 2,530,739 = `loanDrawUsd` 2,530,739 |

したがって、これは**標準初期条件（fixture）と与信パラメータの組み合わせの問題**であり、
三宅さんの指示に従い**私の判断でパラメータを緩めることはしない**。§6に候補を数値で示す。

---

## 5. 代表3ケースの数値トレース（seed `sai3a-grid-001`・BAL）

| 項目 | turn1 健全（初期） | turn3 現金悪化 | turn5 緊急融資が実行された期 |
| --- | --- | --- | --- |
| 申請額 | 10,558,600 | 15,361,893 | 6,046,231 |
| 現金 | 36,147,251 | 15,409,182 | **0** |
| 売掛金 | 42,105,458 | 53,093,502 | 44,324,321 |
| 短期借入 / 長期借入 | 24,000,000 / 31,350,000 | 24,000,000 / 28,050,000 | 2,530,739 / 24,750,000 |
| 純資産 | 100,327,709 | 109,030,429 | 111,511,931 |
| 営業利益（EBITDA代理） | −1,048,291 | 2,881,608 | 3,103,236 |
| 信用区分 / スコア | B / 75.3 | B / 79.7 | **A / 92.6** |
| 担保ベース上限 | 37,270,400 | 44,707,642 | 37,079,958 |
| 収益ベース上限 | 0 | 24,850,836 | 8,557,533 |
| 区分×自己資本上限 | 111,513,600 | 118,460,917 | 164,904,814 |
| **grossLimit** | 37,270,400 | 44,707,642 | 37,079,958 |
| **既存借入残高** | **57,000,000** | **53,700,000** | **50,400,000** |
| **追加借入可能額** | **0** | **0** | **0** |
| grossLimit算定基準※ / 凍結 | collateralBased / false | collateralBased / false | collateralBased / false |
| 承認額 / 否決額 | 0 / 10,558,600 | 0 / 15,361,893 | 0 / 6,046,231 |
| 実行（通常 / 緊急） | 0 / 0 | 0 / 0 | **0 / 2,530,739** |
| 財務制限条項違反 | false | false | false |

※コード上のフィールド名は`bindingConstraint`だが、`grossLimit = min(max(担保,収益),
tierCap)`の`max()`部分でどちらが選ばれたかを表すだけであり、「担保が絞っている」
ことを意味しない（用語の是正は§11-6・§12参照）。このturn1〜5では
`grossLimit = 担保ベース上限`と一致しており、tierCapによる縮小も発生していない
（§13参照）。

**緊急融資へ落ちた理由**: turn5 は初期短期借入 24,000,000 の満期到来期であり、
約定元本＋利息の支払で現金が0になった。通常融資は余力0で承認されないため、
`shortfall = 利息 + 約定元本 − 現金 > 0` が成立し、最後の手段として緊急融資が発動した。
**つまり緊急融資は設計どおりに「通常融資が足りないときだけ」動いている。**

対照として turn6（承認が出た期）: 既存債務 27,280,739 < 担保ベース上限 38,602,092 →
余力 11,321,353 → 申請 28,768,977 のうち **11,321,353 を承認**、
`loanDrawUsd` も同額、短期借入が 2,530,739 → 13,852,092 へ増加。**実行経路は正常。**

---

## 6. 対照実験と、パラメータ候補（**いずれも採用しない**）

### 6-1 初期債務水準を変えた対照実験（3シード×8四半期×5社＝120行）

| 条件 | 申請 | 承認 | 凍結 | 緊急 | 破綻 | 平均grossLimit | 平均既存債務 | 承認総額 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BASE（=P0）現金20M 債務**57M** | 105 | 44 | 0 | 15 | 0 | 38,547,862 | 45,825,366 | 264,175,616 |
| P3/P4 現金35M 債務**70M** | 90 | 31 | 0 | 5 | 0 | 40,798,515 | 54,142,761 | 199,184,305 |
| R1〜R6 現金42M 債務**84M** | 74 | 30 | 0 | 5 | 0 | 39,502,250 | 62,588,671 | 33,365,631 |
| P5/P6/P7 現金60M 債務**95M** | 45 | 16 | 0 | 0 | 0 | 40,919,686 | 70,378,425 | 34,961,743 |
| 【対照】現金60M・債務据置57M | **0** | 0 | 0 | 0 | 0 | 40,951,430 | 42,225,000 | 0 |
| 【対照】現金20M・債務**半減28.5M** | 75 | **75** | 0 | **0** | 0 | 40,931,258 | 32,312,795 | **391,543,766** |

読み取り:

- **債務を半減すると申請の100%が承認され、緊急融資が0件になる。** 承認経路は完全に生きている。
- 債務を上げるほど承認額は単調に崩れる（264M → 199M → 33M → 35M）。
- 現金だけ60Mに増やして債務を据え置くと**申請自体が0件**になる（AIが資金を必要としない）。
  これは「承認されない」のとは別種のゼロであり、区別が必要。

### 6-2 与信パラメータ候補（代表3状態での追加借入可能額）

| 候補 | turn1（債務57.0M） | turn3（債務53.7M） | turn5（債務50.4M） |
| --- | --- | --- | --- |
| 現行 | 0 | 0 | 0 |
| 候補1 `earningsMultiple` 2.0→8.0（年間EBITDA×2相当） | 0 | 0 | 0 |
| 候補2 掛目引上げ（売掛0.7→0.85 / 原料0.4→0.55 / 製品0.3→0.45） | 0 | **4,986,786** | 0 |
| 候補3 候補1＋候補2 | 0 | 4,986,786 | 0 |

**候補1・2・3はほとんど効かない。** 既存債務（50〜57M）が、掛目を大幅に引き上げた後の
担保上限（46M前後）すら上回っているため。

### 6-3 候補5: 信用区分×自己資本を「上限」ではなく「調達源の一つ」にする（構造変更）

現行は `grossLimit = min(max(担保, 収益), 区分×資本)` であり、
**区分×資本の枠（110〜167M）は一度も余力を増やす方向に寄与しない**（FINDIAG-3で固定）。
これを `grossLimit = max(担保, 収益, 区分×資本×w)` に変えた場合:

| 状態 | w=0.25 | w=0.5 | w=0.75 | w=1.0（上限撤廃） |
| --- | --- | --- | --- | --- |
| turn1 健全(tier B) | 0 | 0 | 25,770,360 | 53,360,480 |
| turn3 健全(tier B) | 0 | 6,266,736 | 36,250,104 | 66,233,472 |
| turn5 緊急(tier A) | 0 | 33,233,948 | 75,050,923 | 116,867,897 |

これは**最も効果が大きいが、与信モデルの性格そのものを変える**
（資産担保型から資本基準型へ寄せる）。オーナー判断が要る。

### 6-4 候補4: 標準初期条件の既存債務を下げる（fixtureの変更）

§6-1 の最終行がその実測。**効果は最大で、与信ロジックには一切触れない。**
ただし SAI-3A 標準初期条件の再選定そのものであり、Phase A の未決事項と同じ土俵。

---

## 7. 記録のみ（実装しない）決定事項

**営業活動負荷は HOSO : PD : VAP = 1.0 : 1.2 : 3.0 で確定。**
PDの**営業**負荷を2.0へ倍増することはしない。
これは工場の**労働**負荷（PD = 1.8）とは明確に別物であり、混同しないこと。
本ブランチでは実装変更を行わない。後続作業への申し送りとして記録する。

---

## 8. 未解決・次の作業の推奨

1. **標準初期条件の債務水準と担保構成の整合**（候補4）。
   現行は「担保37M に対し債務57M」で、初期時点から構造的に借入余力ゼロ。
   意図的な厳しさなのか、単に整合が取れていないのかはオーナー確認事項。
2. **区分×自己資本の枠が構造的に不活性であること**（候補5）。
   純資産1億USDの企業が自己資本を根拠に1ドルも借りられない状態が意図どおりか。
3. **`earningsMultiple` が四半期値に対する倍率であること**（候補1）。
   自己整合だが、年間EBITDA換算0.5倍は保守的すぎる可能性。
4. 48コミットスタック側で本診断を再実行し、
   PD係数1.8下でも同じ構造で説明できるかを確認すること（本ブランチでは未実施）。

---

## 9. 前回報告の数値の是正（三宅さんの指摘への回答・全面再計算）

**方針**: §5・§6の数値のうち、以下は**手入力・手計算による転記/再構成**であり、
実行中のライブトレースから直接取っていなかった。今回、全て単一のライブ実行
（`collectTrace("sai3a-grid-001")` のBAL、以下同じ）から機械的に再構成し直した。
数値が変わった箇所は BEFORE（前回）→ AFTER（今回・訂正後）で明示する。

### 9-1 7時点トレース（三宅さん指定の7列）と、指摘された2つのギャップの解消

実装事実（`financing/liquidityClose.ts:96-159`で確認）:
`planQuarterFinancing`は`prevFinanceState`/`prevFinancingState`のみを受け取り、
**当期の実績は関数シグネチャ上そもそも渡せない**。
`existingLoanBalance = prevFinancingState.loanPortfolio.loans`の元本合計＝**前期末残高**が、
そのまま引受(underwriting)の`existingLoanBalanceUsd`引数として渡される
（`liquidityClose.ts:127,153`）。かつ元金返済(`principalPaidCashUsd`)は引受確定後に発生する。
したがって「(1)前期末残高」「(2)引受時渡引数残高」「(3)返済前残高」は、
**別々に動きうる3つの値ではなく、設計上ビットレベルで単一の値**である。

| turn | (1)前期末残高 | (2)引受時渡引数残高 | (3)返済前残高 | (4)通常融資実行※ | (5)緊急融資実行 | (6)元金返済 | (7)当期末残高 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 57,000,000 | 57,000,000 | 57,000,000 | 0 | 0 | 1,650,000 | 55,350,000 |
| 2 | 55,350,000 | 55,350,000 | 55,350,000 | 0 | 0 | 1,650,000 | 53,700,000 |
| 3 | 53,700,000 | 53,700,000 | 53,700,000 | 0 | 0 | 1,650,000 | 52,050,000 |
| 4 | 52,050,000 | 52,050,000 | 52,050,000 | 0 | 0 | 1,650,000 | 50,400,000 |
| **5** | **50,400,000** | **50,400,000** | **50,400,000** | 2,530,739 | 2,530,739 | 25,650,000 | 27,280,739 |
| 6 | 27,280,739 | 27,280,739 | 27,280,739 | 11,321,353 | 0 | 1,650,000 | 36,952,092 |
| 7 | 36,952,092 | 36,952,092 | 36,952,092 | 495,237 | 0 | 4,180,739 | 33,266,590 |
| 8 | 33,266,590 | 33,266,590 | 33,266,590 | 6,609,254 | 0 | 1,650,000 | 38,225,844 |

※列(4)は`fcg.loanDrawUsd`（総実行額）。緊急融資が発動した期は、通常審査は0承認のまま
緊急融資分がそのままここに反映されるため列(5)と一致する（turn5, 二重計上ではない）。

turn(N)の(7)当期末残高は、turn(N+1)の(1)(2)(3)と一致する（例: turn4の(7)=52,050,000 =
turn5の(1)(2)(3)）。ローンポートフォリオが1本の連続した残高として四半期をまたいで
つながっていることが、このライブ実行で確認できる。

**指摘1: turn5の「既存債務 50,400,000」 vs 「短期借入/長期借入 2,530,739+24,750,000=27,280,739」（約23.1M差）**

分類: **参照時点の混在（reference-timing conflation）**。コードのバグではない。

`50,400,000`はturn5の**引受時点**に渡された`existingLoanBalanceUsd`＝**turn4末残高**である
（上表(1)(2)(3)列）。`2,530,739 / 24,750,000`（=27,280,739）は§5の表で
「短期借入 / 長期借入」として印字していた**turn5当期のB/S残高**、
つまり**turn5の元金返済（短期借入の満期一括返済 24,000,000＋通常償還1,650,000等、
計25,650,000）が実行された後の値**である。両者は同一時点の値ではなく、
「引受時点(=前期末)」と「当期の返済後」という**別の時点の残高を1つの表に並べて
表示していた**ことが混同の原因。差額 50,400,000 − 27,280,739 = 23,119,261 は、
turn5に発生した元金返済25,650,000（うち短期借入の一括返済24,000,000が主因）と、
新規実行分2,530,739の差にほぼ一致する（25,650,000 − 2,530,739 = 23,119,261、完全一致）。
**コードは正しく前期末残高を引受に使っており、算出ロジックの誤りではない。**

**指摘2: turn1/turn3の約1.65M差**

分類: **参照時点の混在（reference-timing conflation）**。同じ構造。

turn1: 引受時渡引数残高（=初期fixtureの57,000,000）に対し、turn1のB/S表示は
24,000,000+31,350,000=55,350,000（初期長期借入33,000,000のうち1,650,000が
turn1中に約定償還された後の値）。差額1,650,000 = turn1の元金返済額と厳密に一致。
turn3も同型（53,700,000 vs 52,050,000、差1,650,000 = turn3の元金返済額）。
**いずれも「四半期ごとに約定元本1,650,000が償還される」という設計どおりの動きであり、
表側が引受時点(前期末)と当期末を並べて見せていたために生じた見かけ上のギャップ。**

### 9-2 候補1の矛盾の根本原因（三宅さんの手計算 vs 前回スクリプトの0）

三宅さんの手計算: turn3状態で`grossLimit = min(max(44.708M, 99.403M), 118.461M) = 99.403M`、
`available = max(0, 99.403M − 53.7M) ≈ 45.7M`。前回スクリプトは同じ候補1・turn3で **0** を出力。

**根本原因は選択肢(c): 「候補計算スクリプト内で使われた既存債務/EBITDAが、
その表示turnで実際に引受に使われた値と異なっていた」。**

証拠: `REPRESENTATIVE_STATES`は前回、トレースの印字結果を**目視して手入力**した推測値
だった（`{ ebitdaQ: 3_400_000, ... }`など）。実際にturn3の引受で使われた
`ebitdaLikeQuarterlyUsd`は、`computeBorrowingCapacity`の`earningsBasedLimitUsd`を
現行`earningsMultiple`(2.0)で逆算すると **12,425,418**
（`24,850,836 ÷ 2.0`）であり、手入力値3,400,000とは全く別の値だった
（3,400,000という数字自体、turn3のB/S表示のどの列とも一致しない＝根拠不明の混入値）。

12,425,418 × 8.0 = **99,403,344** — 三宅さんの手計算「99.403M」と完全一致。
これは(c)がスクリプト側の入力ミスであったことの直接証拠であり、
`computeBorrowingCapacity`本体（`borrowingCapacity.ts`）にバグはない
（式は`grossLimit = min(max(collateral, earnings), tierCap)`のまま、
三宅さんの手計算式と一致することも確認済み）。

今回、`REPRESENTATIVE_STATES`は手入力を廃止し、同一のライブ実行から
`ebitdaLikeUsedForUnderwritingUsd = cap.earningsBasedLimitUsd / FINANCING_PARAMETERS_V1.borrowingCapacity.earningsMultiple`
として機械的に逆算する方式に変更した（`scripts/financingCreditDiagnosis.ts`）。
検算として、現行パラメータでこの状態を再計算した`grossLimit`/`availableAdditionalCapacityUsd`が
実トレース行の値と一致することを確認済み（`availableAdditionalCapacityUsd`は3状態とも一致）。

**既知の残存近似（担保上限の内訳）**: `collateralBasedLimitUsd`自体は実トレースの値をそのまま
使えるため正確だが、これを**要素分解**（売掛金／原料在庫(利用可能)／原料在庫(輸送中)／
完成品在庫）する際、`finance/BalanceSheet`が利用可能在庫と輸送中在庫を単一の
`rawMaterialInventory`にしか分離して保持していないため（`borrowingCapacity.ts`冒頭コメント参照）、
本診断スクリプトは輸送中分を0と仮定して近似している。この近似の影響で、
候補2・3・5のうち**担保上限（掛目変更・原料/製品部分）に依存する数値**は、
実際にはやや過大評価（数百万USD規模）の可能性がある。**候補1（収益ベース倍率のみの変更）は
担保上限を一切変更しないため、この近似の影響を受けない**——上記99,403,344の検証は
この近似から独立して成立する。

### 9-3 候補1・2・3・5 — 全中間値つきの再計算（BEFORE→AFTER）

**候補1（`earningsMultiple` 2.0→8.0）**

| turn | 既存債務 | 担保上限 | 収益上限@8.0 | 区分×資本上限 | grossLimit | 追加借入可能額 | BEFORE(前回) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 57,000,000 | 34,623,721※ | 0 | 110,360,480 | 34,623,721 | 0 | 0（一致） |
| 3 | 53,700,000 | 50,473,810※ | **99,403,344** | 119,933,472 | **99,403,344** | **45,703,344** | **0（誤り→訂正）** |
| 5 | 50,400,000 | 41,169,499※ | 34,230,131 | 167,267,897 | 41,169,499 | 0 | 0（一致） |

※担保上限は§9-2の近似（輸送中在庫=0仮定、turn1は担保をturn1自身のB/Sで近似、
turn3/5は前turnのB/Sを使用）を用いた再構成値であり、実トレースの現行値
（37,270,400 / 44,707,642 / 37,079,958）とは一致しない
（原料在庫の利用可能/輸送中の内訳がトレースに保持されていないため）。
**この差は候補1の結論（turn3で45.7M開放、turn1・turn5は変化なし）には影響しない**
（担保上限自体は候補1で変更していないため、変化の有無の判定に使うのは収益上限の増分のみ）。

**候補2（掛目引上げ 売掛0.7→0.85 / 原料0.4→0.55 / 製品0.3→0.45）**、**候補3（候補1+候補2）**

| turn | 候補2 grossLimit | 候補2 available | 候補3 grossLimit | 候補3 available |
| --- | --- | --- | --- | --- |
| 1 | 42,870,752 | 0 | 42,870,752 | 0 |
| 3 | 62,772,356 | 9,072,356 | 99,403,344 | 45,703,344 |
| 5 | 52,087,828 | 1,687,828 | 52,087,828 | 1,687,828 |

（担保上限の近似について§9-2参照。候補2・3の担保依存部分は近似値であり、
実際の掛目適用結果とは数百万USD規模でずれうる。**候補1由来の収益上限効果
（turn3で+45.7M相当）は近似の影響を受けない部分として確定。**）

**候補5（`grossLimit = max(担保, 収益@2.0, 区分×資本×w)`）— 全中間値**

| 状態 | 担保上限 | 収益上限@2.0 | 区分×資本上限(tierCap) | w=0.5: tierCap×w／grossLimit／available | w=0.75: 同 | w=1.0: 同 |
| --- | --- | --- | --- | --- | --- | --- |
| turn1（債務57.0M） | 34,623,721 | 0 | 110,360,480 | 55,180,240／55,180,240／0 | 82,770,360／82,770,360／25,770,360 | 110,360,480／110,360,480／53,360,480 |
| turn3（債務53.7M） | 50,473,810 | 24,850,836 | 119,933,472 | 59,966,736／59,966,736／6,266,736 | 89,950,104／89,950,104／36,250,104 | 119,933,472／119,933,472／66,233,472 |
| turn5（債務50.4M） | 41,169,499 | 8,557,533 | 167,267,897 | 83,633,948／83,633,948／33,233,948 | 125,450,923／125,450,923／75,050,923 | 167,267,897／167,267,897／116,867,897 |

**§6-3（前回）との整合確認**: 前回§6-3のw=0.5/0.75/1.0の数値
（turn1: 0/25,770,360/53,360,480、turn3: 6,266,736/36,250,104/66,233,472、
turn5: 33,233,948/75,050,923/116,867,897）は、今回の全中間値つき再計算と**完全に一致**した。
候補5自体には計算誤りはなかった——三宅さんが「約1,000,000以内で閉じなかった」とされたのは、
前回報告が中間値（担保上限・収益上限・tierCap単体の値）を示さず`w`列の結果だけを
提示していたため、手元での再構成時に**別の担保上限推定値**を使ってしまったことによる
（本来は本セクションの中間値を使えば厳密に再現できる）。

### 9-4 四半期EBITDA vs 年間換算EBITDA — 明示的な仕様判断（コードのバグではない）

現行実装（`liquidityClose.ts:86-88, 128`）:
`ebitdaLike = 前期の営業利益 + 前期の減価償却`（**四半期値そのもの**、年換算しない）。
`earningsBasedLimitUsd = ebitdaLikeQuarterlyUsd × earningsMultiple`（`borrowingCapacity.ts:53`）。
現行`earningsMultiple = 2.0`は、**四半期EBITDA×2.0**であり、年換算では**年間EBITDA×0.5**に相当する
（保守的）。三宅さんの候補1「8.0」提案時の注記「＝年間EBITDA×2」は、
四半期EBITDA×8.0を年換算(×4)した年間EBITDA基準に揃えると年間EBITDA×2.0に相当する、
という意図だったと解釈できる。

| 解釈 | multiplier=2.0（現行） | multiplier=8.0（候補1） |
| --- | --- | --- |
| **四半期EBITDA×multiplier**（現行コードの実装） | turn3: 12,425,418×2.0=24,850,836 | turn3: 12,425,418×8.0=**99,403,344** |
| **年間EBITDA(四半期×4)×multiplier**（もし年換算するなら） | turn3: (12,425,418×4)×2.0=99,403,344 | turn3: (12,425,418×4)×8.0=397,613,376 |

**これはコードのバグではなく、明示的な仕様判断（スペック上の選択）としてオーナー確認が必要な点**:
現行コードは「四半期EBITDA×multiplier」方式であり、`multiplier=8.0`は
（三宅さんの意図どおり）四半期基準のまま倍率を上げるだけで年間EBITDA×2相当の水準
（99,403,344）に達する。**年間換算を別途コードに追加する必要はない** — 三宅さんの
手計算「99.403M」は現に四半期基準のまま`multiplier=8.0`で再現できている
（§9-2参照）。年間換算方式（表の下段）を採用する場合は`multiplier`をさらに4倍に
薄める必要があり、両者は同じ経済的意味を異なる`multiplier`値で表現する等価な選択肢。
**どちらの表現を正とするかは実装時にオーナーが指定すること。**

### 9-5 再判定（候補として提示。一方的に採用しない）

前回報告は「候補1・2・3はほとんど効かない」「fixtureの既存債務引下げ（候補4）が
唯一有効なレバー」と結論していたが、これは§9-2で判明した入力ミスに基づく誤った結論だった。
訂正後の事実:

- **候補1（`earningsMultiple`引上げ）は turn3 のような「EBITDA黒字が積み上がった状態」では
  大きな効果を持つ**（+45.7M）。ただし turn1（EBITDA基準0＝初回四半期）・turn5（緊急融資直前で
  EBITDA基準が薄い状態）では効果ゼロのまま — **EBITDA基準に依存するため、状態依存の効果**であり、
  候補4のように一律には効かない。
- **候補5（自己資本を積極活用）が依然として最も効果が大きい**（turn3でw=0.5でも+6.3M、
  w=1.0で+66.2M）。ただし与信モデルの性格を「資産担保型」から「資本基準型」寄りへ変える
  構造変更であり、パラメータ調整では済まない。
- **候補4（fixture既存債務の引下げ）は依然、唯一「一律・確実に」効くレバー**
  （§6-1実測: 半減で申請の100%が承認）。候補1・5は特定の財務状態（EBITDA黒字の蓄積、
  高い純資産）に依存するため、全社・全turnをカバーする保証がない。
- **これら4つの候補は排他的ではなく併用可能**であり、どれを採用するか・組み合わせるかは
  経済的意味合い（資産担保型を維持するか資本基準型へ寄せるか）を含めてオーナー判断が必要。
  本診断はいずれも実装・採用しない。

### 9-6 48コミットスタック（`/tmp/pd_labor`、PD係数1.8）での再測定

**方法**: `/tmp/pd_labor`はfeature/v2-product-labor-and-pd-mechanizationブランチのまま、
本診断スクリプト（`scripts/financingCreditDiagnosis.ts`）と対応テストを一時的にコピーして
実行しただけ。**コミットは一切行っていない**（実行後に削除し、`git status`が
`nothing to commit, working tree clean`であることを確認済み）。

| 測定項目 | develop/v2ベースライン（本ブランチ） | 48コミットスタック（PD係数1.8） |
| --- | --- | --- |
| turn1 通常融資承認可能額 | 0 | **0（同じ）** |
| 8四半期中の承認あり件数 | 44/120（3シード合計） | **0/120（一度も承認されない）** |
| 余力回復のturn（seed1・BAL） | **turn6で回復**（余力11,321,353） | **8四半期中、一度も回復しない**（turn8まで一貫して0） |
| `underwritingFrozen`初発火 | 一度も発火せず（0/120） | **一度も発火せず（0/120、同じ）** |
| 緊急融資発動 | turn5のみ（1回） | turn5・6・8（3回、より頻発） |

**PD係数1.8のEBITDA経路への間接効果（seed1・BAL）**:

| turn | develop/v2 引受用EBITDA | pd_labor 引受用EBITDA |
| --- | --- | --- |
| 1 | 0 | 0 |
| 3 | 12,425,418 | 8,429,211 |
| 5 | 4,278,767 | 0 |
| 6 | 5,078,236※ | 3,358,197 |

（develop/v2側のturn6値は既存の`収益ベース上限`10,156,472÷2.0から逆算。）

**結論**: PD係数1.8は引受用EBITDA（前期営業利益+前期減価償却）の値を明確に下げ、
一部turnではゼロまで落とす（**測定可能な間接効果あり**）。しかし
【用語是正・§11-6・§12参照】`grossLimit`の算定基準（`bindingConstraint`）は
develop/v2・pd_labor両方で一貫して`collateralBased`——すなわち
**担保ベース値が収益ベース値を上回り、grossLimit算定の基礎値として選択され続けた**
（担保が「絞っている」「律速している」わけではない。§13の集計では実際、
両スタックとも120行中0件が`grossLimit>0`かつtierCapに縮められる状態にすら
至っておらず、`grossLimit`自体はほぼ全行で担保ベース値と同値だった）。
収益ベース値が担保ベース値を上回った行は3シード×8ターン×5社のいずれにも
存在しなかった。したがって**「PD係数1.8がEBITDA経由で承認可否に直接影響する」という
仮説は今回のデータでは棄却される**——影響は数値レベルでは存在するが、
`grossLimit`の算定基準が一貫して担保ベース値である現行の状態では、
承認結果を左右していない。

一方で、pd_labor側は**8四半期を通じて一度も承認が回復しない**（develop/v2はturn6で回復）。
これはEBITDA経由の間接効果ではなく、担保上限側の推移の違い（原料在庫・完成品在庫の
価値がPD係数1.8下でどう積み上がるかの違い）に起因する可能性が高いが、
**この一段深い原因分析は本タスクのスコープ外**（本タスクは測定のみが要求範囲）。

---

## 10. §9是正の要約（分類一覧）

| # | 指摘内容 | 分類 | 説明 |
| --- | --- | --- | --- |
| 1 | turn5「既存債務50.4M」vs「短期/長期27.28M」（差約23.1M） | 参照時点の混在 | 前者=引受時点(前期末)、後者=当期返済後。差額は当期元金返済25.65M−新規実行2.53Mに厳密一致 |
| 2 | turn1/turn3の約1.65M差 | 参照時点の混在 | 前者=引受時点(前期末)、後者=当期B/S。差額=当期約定元本償還1,650,000に厳密一致 |
| 3 | 候補1・turn3が0（三宅さん手計算99.403M/45.7Mと矛盾） | **スクリプトの入力ミス（原因(c)）** | `REPRESENTATIVE_STATES`のebitdaQ手入力値(3,400,000)が実際の引受用EBITDA(12,425,418)と無関係の値だった。本体コードにバグなし |
| 4 | 候補5のw=0.5/0.75/1.0の数値が約1,000,000以内で閉じない | 報告側の情報不足 | 前回報告が中間値（担保上限・収益上限・tierCap単体）を示していなかったための手元再構成誤差。全中間値を示した今回の再計算で§6-3の数値と完全一致を確認 |
| 5 | 四半期EBITDA vs 年間換算EBITDA | **仕様判断（バグではない）** | 現行は四半期EBITDA×multiplier。multiplier=8.0のまま三宅さんの手計算(99.403M)と一致する。年間換算方式にする場合はmultiplierを別途調整が必要（オーナー判断） |

**本番の計算式・パラメータ・閾値への変更は一切行っていない。** `borrowingCapacity.ts`・
`bankUnderwriting.ts`は読み取りのみ。`liquidityClose.ts`には§11で説明する
observation-onlyフック（デフォルト未登録＝既存動作と完全に同一）を追加したが、
これは計算式・戻り値・RNG消費・意思決定を一切変更しない（SNAP-1でシミュレーション結果の
完全同一性を検証済み）。§11以降で詳細を報告する。

---

## 11. 同一時点スナップショット基盤（三宅さんの是正指示・2026-08-03）

### 11-1 背景と問題意識

§9の是正で「参照時点の混在」を解消したが、その是正自体もB/S行やturn間の
手動再構成（前turnのB/S値を担保入力として近似する等）に依存しており、
近似の域を出ていなかった。三宅さんの指摘（tierCap 118.461M vs 119.933Mの不一致等）を
受け、**近似・再構成を一切行わず、`computeBorrowingCapacity`へ実際に渡された入力
オブジェクトそのものを、その呼び出しの場でそのまま捕捉する**基盤を構築した。

### 11-2 捕捉の仕組み（observation-onlyフック）

`app/lib/v2/financing/liquidityClose.ts`に、以下の2つの登録式コールバックを追加した:

- `setUnderwritingSnapshotObserver(observer)`: `planQuarterFinancing`内で
  `computeBorrowingCapacity`へ実際に渡す引数オブジェクト（`const borrowingCapacityInput`として
  一度束縛してからそのまま渡す。渡し方自体は従来のインライン組み立てを変数化しただけ）と、
  その戻り値・信用スコア・財務制限条項・審査結果を、計算直後にそのまま渡す。
- `setLoanRollForwardObserver(observer)`: `closeQuarterWithFinancing`内で、
  前期末残高（ST/LT内訳）・通常融資実行額・緊急融資実行額・約定元本・実際元本返済額・
  当期末残高（ST/LT内訳）を、計算完了直後にそのまま渡す。

**本番挙動への影響がないことの保証**:

1. オブザーバーは`let`変数に保持され、デフォルトは`undefined`。`if (observer)`の
   内側でしか呼ばれないため、未登録時は既存コードと完全に同一の処理しかしない。
2. コールバックは値を読み取って配列にpushするだけで、会社状態・返り値・RNGを
   一切書き換えない。
3. `SNAP-1`テスト（`app/lib/v2/financing/__tests__/underwritingSnapshotInvariants.test.ts`）で、
   同一seedをオブザーバー登録あり/なしで実行し、`result.history`
   （財務・資金繰り・生産・市場等の全結果）が**完全に同一**であることを検証済み
   （`JSON.stringify`による厳密比較）。
4. `SNAP-2`で、同一seedでの2回実行でスナップショット自体も完全に同一であること
   （決定性）を検証済み。

新しい診断スクリプト `scripts/financingSnapshotDiagnosis.ts` がこのフックを使い、
1社×1turnぶんの与信スナップショットとロールフォワードスナップショットを
`companyId × period`で1対1結合した`CombinedSnapshot`を構築する
（結合の妥当性根拠: `companyLab/runner.ts`の呼び出し実装事実として、
`planQuarterFinancing`と`closeQuarterWithFinancing`は同一ターン内で
同一の`state.financingState.companies`から取得した同一の`prevFinancingState`参照を
渡されて呼ばれ、両呼び出しの間でこの配列は変更されない。`SNAP-4`で
`companyId`/`period`の対応関係を検証済み）。

### 11-2a オブザーバー機構のコードレビュー向け詳細（三宅さんの是正指示・2026-08-04）

**登録/解除の方法**: `setUnderwritingSnapshotObserver(fn)` / `setLoanRollForwardObserver(fn)`
にコールバックを渡すと登録、`undefined`を渡すと解除。呼び出し側は必ず
`try { ...シミュレーション実行... } finally { setXxxObserver(undefined); }`の形で、
finally節で解除する（`scripts/financingSnapshotDiagnosis.ts`の`collectSnapshots`、
`app/lib/v2/financing/__tests__/underwritingSnapshotTestHelpers.ts`の`runOnce`を参照）。
これにより、シミュレーション実行中に例外が投げられても、オブザーバーが
登録されたまま他の実行・他のテストへ残ることがない。

**オブザーバー自体が例外を投げた場合**: `liquidityClose.ts`内の`invokeObserverSafely`
ヘルパー関数が、オブザーバー呼び出しを`try/catch`で囲む。オブザーバーが例外を
投げても`console.error`に記録するだけで処理を継続し、**本番の四半期クローズ処理
（`planQuarterFinancing`・`closeQuarterWithFinancing`の残りの計算・戻り値）へは
一切伝播しない**。これは今回追加した安全対策であり（三宅さんの是正指示4番への
対応）、`SNAP-12`テストで、意図的に例外を投げるオブザーバーを登録した状態で
8四半期のシミュレーションを実行し、(a) シミュレーション全体が例外を投げずに
最後まで完了すること、(b) オブザーバーは実際に呼ばれたこと（例外を投げる前の
処理は実行されたこと）を確認済み。

**テスト分離**: `underwritingSnapshotObserver`/`loanRollForwardObserver`は
モジュールスコープの`let`変数であり、プロセス内でグローバルに共有される。
本ブランチのテスト（`underwritingSnapshotInvariants.test.ts`等）は、各テストが
`runOnce()`という1つの同期的な関数呼び出しの中で「登録→実行→finally解除」を
完結させており、かつ`node --test`はデフォルトで同一ファイル内のテストを
直列実行するため、現状のテストスイートでは競合は発生しない。
**将来「複数テストを並行実行するオプション」を有効化する場合は、この
グローバル状態が競合点になりうる**（対策案は`liquidityClose.ts`の
`setUnderwritingSnapshotObserver`のドキュメントコメントに記載）。

**Pass1/Pass2との関係（二重捕捉の防止）**: `closeQuarterWithFinancing`内部では
`finance/quarterClose.ts`の`closeFinancialQuarter`をPass1（予備）・Pass2（確定）の
2回呼ぶが、`loanRollForwardObserver`の発火はこの2回呼び出しの**外側**、
`closeQuarterWithFinancing`全体の最後（return直前）に1箇所だけ置かれている。
したがって1社×1turnの引受判断につき、オブザーバーは**必ず1回だけ**発火する
（Pass1/Pass2それぞれで発火するわけではなく、二重捕捉は構造的に発生しない）。

**未登録時の挙動（改めて明示）**: `setUnderwritingSnapshotObserver`は
`planQuarterFinancing`内で`invokeObserverSafely(underwritingSnapshotObserver, ...)`
として呼ばれるが、`invokeObserverSafely`の実装は`if (!observer) return;`から
始まる——未登録時（`observer === undefined`）はこの1行のガード以外、
一切の追加処理（オブジェクト構築・関数呼び出し）を行わない。
`setLoanRollForwardObserver`側は、外側の`if (loanRollForwardObserver)`ガードが
既にこの役割を果たしており（未登録時は`priorShortTermLoansUsd`等の集計
そのものがスキップされる）、`invokeObserverSafely`はその内側で追加の
例外隔離のみを行う。**いずれの経路でも、未登録時に本番処理へ追加される
コストはガード判定1回のみ（実質ゼロ）である。**

### 11-3 同一時点検証: 現行パラメータでの完全一致

`scripts/financingSnapshotDiagnosis.ts`を`npx tsx scripts/financingSnapshotDiagnosis.ts --candidates`で
実行すると、3シード×8四半期×5社＝120行**全て**で、捕捉した`borrowingCapacityInput`を
現行パラメータで再計算した結果（`collateralBasedLimitUsd`・`earningsBasedLimitUsd`・
`creditTierCapUsd`・`grossLimitUsd`・`existingLoanBalanceUsd`・
`availableAdditionalCapacityUsd`・`underwritingFrozen`・`bindingConstraint`の全8項目）が、
本番の実行結果と**floating-point許容誤差内で完全一致**する
（`SNAP-3`テストでも同じ検証を実施し全項目一致を確認）。

§9の近似（原料在庫の輸送中/利用可能の内訳を0と仮定）は、今回は**不要になった**——
`borrowingCapacityInput`は実際に渡された`CollateralInput`オブジェクトそのものであり、
分解・再構成する必要がないため。実際、今回のturn1/3/5の担保上限は
`37,270,400 / 44,707,642 / 37,079,958`と、§5の最初のトレース（Excel/表示から直接取得した値）と
一致し、§9の近似値（`34,623,721 / 50,473,810 / 41,169,499`）とは異なる
——**今回の値が正であり、§9の近似値はこの近似に起因する誤差だった**ことが確定した。

### 11-4 tierCap不一致（118.461M vs 119.933M）の根本原因の確定

三宅さんが指摘した具体的な不一致——turn3の実測tierCapが以前は約118.461Mだったのに、
候補比較表では119.933Mになっていた——の原因を特定した。

`creditTierCapUsd = max(0, totalEquityUsd) × creditTierEquityMultiple[tier]`
（`borrowingCapacity.ts`）。turn3・tier Bで`multiplier=1.1`。

- **今回の同一時点スナップショットでの実測**: `totalEquityUsd = 107,691,743`
  （`borrowingCapacityInput.totalEquityUsd`をそのまま参照）。
  `107,691,743 × 1.1 = 118,460,917.3` → tierCap = **118,460,917**（実測と一致）。
- **§9の候補スクリプトで使っていた値**: `equity: 109,030,429`
  （turn3自身の`totalEquityUsd`表示値を使っていた）。
  `109,030,429 × 1.1 = 119,933,471.9` → tierCap = **119,933,472**（三宅さんが指摘した値と一致）。

**原因は「equityの参照時点」**（三宅さんが挙げた選択肢の1つ）。
`planQuarterFinancing`の実装（`liquidityClose.ts`）では
`totalEquity = fs.capitalStock + fs.retainedEarnings`が`input.prevFinanceState`
（前期末の財務状態）から計算される——既存借入残高・EBITDA代理と全く同じ
「前期末＝引受時点」の参照規律に従っている。turn3の実測値107,691,743は
**turn2末の純資産**であり、§9のスクリプトが使った109,030,429は**turn3自身の
（当期末に近い）純資産表示値**だった。既存借入残高・EBITDA代理では§9是正時に
このタイミングのずれに気づいていたが、equityについては見落としていた。

これは**コードのバグではない**（`planQuarterFinancing`は一貫して前期末値を参照する
設計を守っている）。§9の候補スクリプトが、この一貫した規律から**equityだけ**外れて
当期値を使ってしまっていた診断側の入力ミスである。今回のスナップショット基盤は
`borrowingCapacityInput`を分解せずそのまま使うため、この種の取り違えが
構造的に起こり得ない。

### 11-5 7点ロールフォワード（同一スナップショットのみで構成）

`LoanRollForwardSnapshot`は、`closeQuarterWithFinancing`内の実際の計算過程
（`priorLoans`・`normalDrawUsd`・`emergencyDrawUsd`・`scheduledPrincipalDue`・
`waterfall.totalPrincipalPaidUsd`・`endingShortTermLoansUsd`・`endingLongTermLoansUsd`）を
そのまま捕捉したものであり、B/S表示からの再構成を一切含まない。

代表ケースB（turn5・緊急融資発動）での実測:

| 項目 | 値 |
| --- | --- |
| (1)(2)(3) 前期末=引受時=返済前 | ST 24,000,000 / LT 26,400,000 / 計 50,400,000 |
| (4) 通常融資実行 | 0 |
| (5) 緊急融資実行 | 2,530,739 |
| 合計実行額（検算: 通常+緊急） | 2,530,739（一致） |
| 約定元本 | 25,650,000 |
| (6) 実際元本返済 | 25,650,000（約定どおり全額支払えた） |
| 約定利息 / 実際利息支払 | 1,134,000 / 1,134,000 |
| (7) 当期末 | ST 2,530,739 / LT 24,750,000 / 計 27,280,739 |
| 検算: 前期末+通常+緊急-実際元本 | 50,400,000+0+2,530,739-25,650,000 = 27,280,739（当期末と一致） |

`normalDrawUsd`は`emergencyDrawUsd`と別列であり、緊急融資のみが発動したこのturnで
`normalDrawUsd=0`であることを確認済み（二重計上なし。`SNAP-7`で全行について検証）。

### 11-6 用語の是正: 「拘束条件」の使い方

**是正**: `grossLimit = min(max(collateralBasedLimit, earningsBasedLimit), creditTierCap)`は
`max()`を含むため、「担保上限 > 収益上限」は「担保が絞っている（拘束している）」ことを
**意味しない**。正しい表現は「grossLimitの基準として担保ベース値が選ばれた」
「担保ベース値が収益ベース値を上回った」である。「拘束条件(binding constraint)」という
語は、実際に上限を縮める側——`creditTierCap`（外側の`min`）、または既存債務控除
（`availableAdditionalCapacityUsd = max(0, grossLimit - existingLoanBalance)`）——にのみ
用いる。§5・§6・§9で「binding: collateralBased」等と表示していた既存の`bindingConstraint`
フィールド自体（コード内の命名）は変更しないが、**このドキュメントでの説明文言**は
本セクション以降、`describeGrossLimitBasis()`（`scripts/financingSnapshotDiagnosis.ts`）の
表現に統一する。§5・§9で「拘束条件: collateralBased」という表現を使った箇所は、
「grossLimitの基準として担保ベース値が選ばれた（tierCapが実際に絞っているわけではない）」
という意味に読み替える必要がある——コードの`bindingConstraint`フィールド名自体が
`max()`部分の内訳を指す名前になっており、真に上限を縮める`min(..., tierCap)`とは
別の概念であることに、本セクションで初めて気づいた。**用語自体の整理が必要
（コード変更は不要。フィールド名の意味を正しく説明するドキュメント側の是正で足りる）。**

### 11-7 候補1（初期債務・fixtureの調整）— 現金と分離した単独変化

**【方法論の明示（三宅さんの是正指示）】本セクションの全ての数値は、
静的スナップショット再計算（§11-3〜11-6・11-8〜11-10と同じ、1時点の
`borrowingCapacityInput`を仮の値で置き換えて`computeBorrowingCapacity`を
呼び直すだけの手法）では**ない**。`scripts/financingCreditDiagnosis.ts`の
`collectTrace(seed, override)`は、`override`で初期fixtureの`shortTermLoans`/
`longTermLoans`/`cash`を実際に変更した上で、`initializeUnifiedCompanyLabFromTemplate`
から`runFromInit`まで**8四半期分をturnごとに実際に再シミュレーション**する
（AIの意思決定・市場・生産・与信判断の全てが、変更した初期状態から
改めて計算し直される）。したがって「緊急融資0件」「turn1平均available」
「承認44→75件」等の数値は、**将来時点（turn2以降）の挙動を含む本番の
再シミュレーション結果**であり、静的な1時点の再計算では原理的に
観測できない性質の値である。**この区別により、以下の全ての数値は
「本番再シミュレーション結果」に分類される（Exact live valueの一種。
§14参照）。**

前回までは初期現金と初期債務を同時に動かした変異点（P3/P4/R1〜R6/P5〜P7）が
混在しており、「何が効いているか」を切り分けられていなかった。今回は
**債務だけを動かす変異**と**現金だけを動かす変異**を完全に分離して再測定した
（`scripts/financingCreditDiagnosis.ts`の`collectTrace`に`FinanceFixtureOverride`で
片方だけを渡す。3シード×8四半期×5社=120行、全て実際にターンごとに
再シミュレーションした結果）。

**債務のみ変化（現金20Mで固定）**

| 条件 | 申請 | 承認 | 緊急 | 承認総額 | turn1平均available |
| --- | --- | --- | --- | --- | --- |
| 現行（債務57.0M） | 105 | 44 | 15 | 264,175,616 | 0 |
| 債務-10%（51.3M） | 105 | 41 | 6 | 279,861,494 | 0 |
| 債務-20%（45.6M） | 81 | 41 | 0 | 285,177,815 | 0 |
| 債務-30%（39.9M） | 76 | 46 | 0 | 381,832,405 | 0 |
| 債務-50%（28.5M） | 75 | **75**（100%） | **0** | 391,543,766 | 8,770,400 |

**現金のみ変化（債務57.0Mで固定）**

| 条件 | 申請 | 承認 | 緊急 | 承認総額 |
| --- | --- | --- | --- | --- |
| 現行（現金20M） | 105 | 44 | 15 | 264,175,616 |
| 現金35M | 61 | 16 | 0 | 222,413,579 |
| 現金60M | **0** | 0 | 0 | 0 |

読み取り:

- **turn1時点の追加借入可能額が0でなくなるのは、債務を約50%削減した場合のみ**
  （-30%ではまだturn1平均available=0）。閾値は-30%〜-50%の間にある。
- 債務削減は緊急融資件数を単調に減らし（15→6→0）、承認総額を増やす。
  **B/Sの経済的妥当性**: 標準初期条件の純資産は約1億USDであり、
  債務57M（対純資産比57%）は極端に高いわけではないが、**担保ベース上限
  （約37M）に対しては既に上回っている**——つまり「担保評価に対して重すぎる」
  というより「掛目後の担保評価そのものが薄い」ことが主因（§6-1）。
- **現金を増やすだけでは問題は解決しない**——むしろ現金60Mでは
  AIが資金需要自体を感じなくなり申請が0件になる（「承認されない」ではなく
  「そもそも借りようとしない」という別種のゼロ）。**現金だけを動かす変異は
  候補1の効果測定には不適切**（初期の資金需要そのものを変えてしまうため）。
- **難易度が下がりすぎるか**: 債務-50%は緊急融資を完全に0にする
  （＝資金繰りに関する経営判断の重要性が消える）。-20%〜-30%であれば
  緊急融資は0〜数件残り、経営判断の余地を保ちながら通常融資の承認率を
  改善できる（-20%で承認率41/81=51%、-30%で46/76=61%）。

### 11-8 候補2（`earningsMultiple`変更）— 同一スナップショットでの再計算

現行2.0、候補として4.0・6.0・8.0を比較（§11-3の同一時点検証で保証された
`borrowingCapacityInput`をそのまま使用。turn1・turn3・turn5・BAL・seed1）。

| turn | 既存債務 | EBITDA代理 | mult=2.0 available | mult=4.0 | mult=6.0 | mult=8.0 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 57,000,000 | 0 | 0 | 0 | 0 | 0 |
| 3 | 53,700,000 | 12,425,418 | 0 | 0 | 0 | **45,703,344** |
| 5 | 50,400,000 | 4,278,766 | 0 | 0 | 0 | 0 |

**turn3の`mult=8.0`（earningsBasedLimit≈99,403,344、available≈45,703,344）は、
三宅さんの手計算と完全に一致し、かつ§9-2の逆算値・今回の同一スナップショット値
（EBITDA代理=12,425,418）とも一致する。この結果は今回が最も信頼できる
（近似・逆算を経ず、`computeBorrowingCapacity`へ実際に渡された入力をそのまま
使って再計算したものであるため）——§9時点の結論を上書きするものではなく、
確認・補強するものである。**

読み取り: `earningsMultiple`の引き上げは、**EBITDAが十分に黒字で蓄積した状態
（turn3）でのみ**効果を持つ。turn1（初回四半期・EBITDA代理=0）、turn5
（緊急融資直前・EBITDA代理が薄い）では、倍率をどれだけ上げても効果ゼロ。
**EBITDA基準に依存する状態依存の効果であり、候補1（fixture一律修正）のような
「全社・全turn一律の効果」ではない。**

### 11-9 候補3（四半期EBITDA vs 年間換算EBITDA）— 経済的に同一水準の2つの表現

現行実装は`ebitdaLikeQuarterlyUsd × earningsMultiple`（四半期値そのもの、
年換算しない。`liquidityClose.ts`のコメント・実装で確認済み）。

三宅さんの整理どおり、**「四半期EBITDA×8」と「年換算EBITDA(四半期×4)×2」は
同じ経済水準を指す**（12,425,418×8 = 99,403,344 = (12,425,418×4)×2）。
これは**候補2の別表現であり、候補2とは独立した「候補3」という別の経済モデルではない**
——多重に倍率をかけてはならない、という三宅さんの注意点どおり。もし将来、
コードを「年換算EBITDA×multiplier」という表現に書き換えるなら、
`multiplier`は現行の2.0のままではなく、**現行の経済水準を保つには
`multiplier ÷ 4`（例: 現行2.0相当を保つなら年換算基準でmultiplier=0.5）**
としなければならない。**現時点ではコードを年換算表現に書き換える提案はしない**
（四半期基準のままでも三宅さんの手計算と一致することが確認できたため、
書き換えの実益がない）。

### 11-10 候補5（`grossLimit = min(max(担保, 収益, tierCap×w), tierCap)`）— 全中間値

外側の`min(..., tierCap)`を維持したまま（tierCap自体を撤廃しない）、
自己資本を「上限」ではなく「調達源の一つ」として`max()`の内側に加える変異。

| 状態 | 担保 | 収益@2.0 | tierCap | w=0.25 | w=0.50 | w=0.75 | w=1.00 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| turn1（債務57.0M） | 37,270,400 | 0 | 111,513,600 | avail=0 | avail=0 | avail=26,635,200 | avail=54,513,600 |
| turn3（債務53.7M） | 44,707,642 | 24,850,836 | 118,460,917 | avail=0 | avail=5,530,459 | avail=35,145,688 | avail=64,760,917 |
| turn5（債務50.4M） | 37,079,958 | 8,557,533 | 164,904,814 | avail=0 | avail=32,052,407 | avail=73,278,610 | avail=114,504,814 |

（各状態のtierCap×w・max()結果・grossLimit・既存債務は§11-3で保証された同一スナップショットの
`borrowingCapacityInput`から`scripts/financingSnapshotDiagnosis.ts --candidates`で算出。
`w=1.00`は`tierCap×w=tierCap`となるため実質的に「tierCapをそのまま`max()`に含める」ことと同じ。）

読み取り: **候補5は3状態全てで候補1（earningsMultiple）より広く効く**
（turn1・turn5でも`w≥0.5〜0.75`で効果が出る）。ただし「自己資本を担保・収益と
並列の調達源にする」という**与信モデルの性格そのものを変える構造変更**であり、
パラメータのチューニングでは済まない。

**参考（候補5とは別枠）**: tierCapを完全撤廃する場合（`grossLimit = max(担保, 収益, tierCap×w)`、
外側の`min`なし）は、turn1のw=1.0で`available=54,513,600`など、上表と数値上は
同じ結果になる（w=1.0のときtierCap×w=tierCapで、`min(x,tierCap)`と`x`が一致するため）。
ただしw<1.0の場合や、将来equityが極端に大きい会社が現れた場合には両者は乖離しうる。
**tierCap撤廃は候補5とは明確に別の変異であり、本診断では推奨も実装もしない
（参考情報としてのみ記載）。**

---

## 12. 候補の評価基準（「借入額が増えた」だけで見ない）

| 観点 | 候補1（fixture債務削減） | 候補2（earningsMultiple↑） | 候補5（equity基準追加） |
| --- | --- | --- | --- |
| 健全企業が通常融資を使えるか | ○（-50%で100%） | △（EBITDA黒字企業のみ） | ○（w≥0.5で幅広く） |
| 不健全企業への過剰融資リスク | 低（初期状態一律の変更で個社の健全性判定は変えない） | 低（EBITDA黒字が条件） | **中**（純資産さえあれば収益力が薄くても借りられる） |
| 既存債務が大きい企業への追加融資量 | 直接的（初期債務そのものを下げる） | 状態依存（turn3のような黒字蓄積期のみ大） | 大きい（w=1.0でturn1から+54.5M) |
| 担保重視 vs 収益重視のどちらへ寄せるか | 中立（両モデルは変えない） | 収益重視へ | **資本重視へ**（新しい第3の軸） |
| 信用区分の意味を保つか | 保つ | 保つ | **薄める**（tierが低くても純資産があれば借りられてしまう） |
| 緊急融資への依存を下げるか | 大幅に下げる（-50%で0件） | 限定的（turn3のような黒字期のみ） | turn5でも効果あり（w≥0.5で+32M以上） |
| 破綻頻発の防止 | 効果あり（緊急融資減） | 限定的 | 効果ありそうだが未検証（返済能力を伴わない借入増の副作用は要検証） |
| 資本投資(capex/PD機械化)の資金現実性 | 改善（初期の資金繰り改善が投資余力に波及） | turn3以降の黒字企業のみ改善 | 最も改善（自己資本の大きい企業ほど投資しやすくなる） |
| 初期状態のみの修正か、全期間の与信モデルの修正か | **初期状態のみ**（fixture/初期B/Sの是正） | **全期間**（収益評価の重み変更） | **全期間**（与信モデルに新しい基準を追加） |
| プレイヤーへの説明しやすさ | 易（「初期の借金が担保評価に対して重すぎた」） | 中（「黒字が続けば借りやすくなる」） | 中〜難（「自己資本があれば信用区分によらず借りられる」は難しい） |
| ゲーム難易度・経営判断の重要性の維持 | **難易度を大きく下げうる**（-50%は緊急融資を消す） | 維持（EBITDA黒字化という経営努力が必要） | 維持しつつ間口を広げる（自己資本の蓄積という別の経営努力軸が生まれる） |

**候補1（fixture修正）・候補2（earningsMultiple変更）・候補5（equity基準追加）は、
性質の異なる3種類の変更であり、単純に「借入枠拡大策」として一緒くたに扱うべきではない**:

- 候補1は**初期状態（fixture）の是正**——標準初期条件が意図せず「初期時点で
  構造的に借入余力ゼロ」という状態を作っていたことへの対処。
- 候補2は**全期間の収益評価の重み変更**——EBITDA黒字企業がより多く借りられるようにする、
  与信モデル内の既存の軸（収益ベース）の強化。
- 候補5は**与信モデルに新しい軸（自己資本）を追加する構造変更**——現行にない
  評価軸を導入するため、影響範囲が最も広く、信用区分の意味を薄める副作用リスクも
  独自に持つ。

---

## 13. develop/v2 と 48コミットスタック（PD係数1.8）の比較（読み取り専用測定）

**テストの実行方法（両スタックで別々に実行し、両方ともgreenになることが合格条件）**:

```
# develop/v2側（本ブランチ、/tmp/fin_diag）— スタック非依存の不変条件 + develop/v2固有の期待値
cd /tmp/fin_diag
node --test --import tsx app/lib/v2/financing/__tests__/underwritingSnapshotInvariants.test.ts   # 10/10 pass
node --test --import tsx app/lib/v2/financing/__tests__/underwritingSnapshotDevelopV2.test.ts     # 3/3 pass

# pd_labor側（48コミットスタック、/tmp/pd_labor）— 診断オーバーレイを一時コピーして実行、終了後に復元
# (underwritingSnapshotPdLabor.overlay.tsの冒頭コメントに手順の詳細あり)
cd /tmp/pd_labor
node --test --import tsx app/lib/v2/financing/__tests__/underwritingSnapshotInvariants.test.ts   # 10/10 pass（同じ不変条件ファイルをコピーして実行）
node --test --import tsx app/lib/v2/financing/__tests__/underwritingSnapshotPdLabor.overlay.ts       # 4/4 pass
```

**financing配下のテストファイル数の内訳（用語の揺れ防止のため明記）**:
`app/lib/v2/financing/__tests__/*.test.ts`は**9ファイル**
（`bankUnderwriting.test.ts`=7、`borrowingCapacity.test.ts`=7、
`borrowingCapacityStructure.test.ts`=9、`covenant.test.ts`=7、
`creditScore.test.ts`=8、`interestRateAndSchedule.test.ts`=9、
`liquidityClose.test.ts`=17、`underwritingSnapshotDevelopV2.test.ts`=3、
`underwritingSnapshotInvariants.test.ts`=10、合計77件、全てpass）。
これとは別に、`underwritingSnapshotPdLabor.overlay.ts`（4件）と
`underwritingSnapshotTestHelpers.ts`（テストファイルではない共有ヘルパー、
0件）が同じ`__tests__`ディレクトリに存在するが、拡張子が`.test.ts`で
ないため上記9ファイルのカウントには含まれない（§11-2a・pd_labor向け
オーバーレイの説明を参照。develop/v2の`npm test`グロブから意図的に除外している）。

`/tmp/pd_labor`（`feature/v2-product-labor-and-pd-mechanization`、HEAD `5f1fa87`）へ
`liquidityClose.ts`の観測フック・`financingSnapshotDiagnosis.ts`・
`underwritingSnapshotTestHelpers.ts`・`underwritingSnapshotInvariants.test.ts`・
`underwritingSnapshotPdLabor.overlay.ts`を**一時的にコピー**して測定し、
測定後に全て削除、`git checkout --`でliquidityClose.tsを復元し、
`git status`が`nothing to commit, working tree clean`・HEAD `5f1fa87`が
不変であることを確認済み（**コミットは一切行っていない**。手順の詳細は
`underwritingSnapshotPdLabor.overlay.ts`冒頭のコメントを参照）。

| 測定項目 | develop/v2（本ブランチ） | 48コミットスタック（PD係数1.8） |
| --- | --- | --- |
| 同一時点検証（120行中の完全一致数） | 120/120 | **120/120（同じく完全一致）** |
| 申請件数（3シード×8Q×5社） | 105/120 | 105/120（同じ） |
| 承認件数 | 44 | **0**（1件も承認されない） |
| 審査凍結（underwritingFrozen）件数 | 0 | 0（同じ。一度も発火しない） |
| 緊急融資件数／総額 | 15件／約2.65億USD相当 | **45件／320,737,759USD**（3倍） |
| turn1(2015Q1)の追加借入可能額（BAL, 3シード平均） | 0 | 0（同じ） |
| 8四半期中の追加借入可能額の推移（BAL, 3シード） | turn6(2016Q2)で回復 | **8四半期を通じて一貫して0**（一度も回復しない） |
| turn3のEBITDA代理（同一スナップショット, BAL seed1） | 12,425,418 | 8,429,211（-32%） |
| SNAP-8（健全fixtureは承認される）テスト | pass | **fail**（承認が1件もないため） |
| SNAP-11（turn3・mult8.0で三宅さんの手計算と一致）テスト | pass | fail（develop/v2固有の回帰ロックテストのためpd_labor値には非適用。テスト自体の欠陥ではない） |

**結論**: PD係数1.8は引受用EBITDA（前期営業利益+前期減価償却）を明確に押し下げる
（測定可能な間接効果あり、turn3で-32%）。しかし`bindingConstraint`（正しくは
「grossLimitの基準」）はdevelop/v2・pd_labor両方で一貫して担保ベース値が選ばれており、
収益ベース値が上回った行は今回の120行中1つも存在しなかった。**したがって
「PD係数1.8がEBITDA経由で承認可否に直接影響する」という仮説は、今回のデータでは
支持されない**——影響は数値レベルで存在するが、現行の与信構造
（担保ベース値が支配的な水準にある）の下では承認結果を左右していない。

一方、pd_labor側は**8四半期を通じて一度も承認が回復しない**（develop/v2はturn6で回復）。
これは担保上限側の推移の違い（原料在庫・完成品在庫の価値がPD係数1.8下でどう
積み上がるかの違い）に起因する可能性が高いが、**この一段深い原因分析は
本タスクのスコープ外**（PD係数の変更は禁止されており、本タスクは測定のみが範囲）。

### 13-1 grossLimit算定基準・既存債務控除・凍結の内訳集計（両スタック側並び）

3シード×8四半期×5社＝120行（`UnderwritingSnapshot`、両スタックとも同一手法・
同一シード集合）を対象に、以下5種類の行数を集計した
（用語は§11-6・§12の是正を反映——「担保が絞る」ではなく「grossLimitの算定基準として
選択された」という表現を用いる）。

| 集計項目 | develop/v2 | pd_labor（PD係数1.8） |
| --- | --- | --- |
| `max(collateralBasedLimit, earningsBasedLimit) > creditTierCap`（tierCapが実際にgrossLimitを縮小した行） | **0** | **15** |
| `collateralBasedLimit ≥ earningsBasedLimit`（担保ベース値がgrossLimitの算定基準として選択された行） | 120 | 120 |
| `earningsBasedLimit > collateralBasedLimit`（収益ベース値が算定基準として選択された行） | 0 | 0 |
| `grossLimit > 0` かつ `grossLimit − existingLoanBalance ≤ 0`（余力自体はあったが既存債務控除で0になった行） | 76 | **120（全行）** |
| `underwritingFrozen`（信用区分E／重大延滞／支払不能によるブロック） | 0 | 0 |

読み取り:

- **両スタックとも、収益ベース値がgrossLimitの算定基準として選ばれた行は1件もない**
  ——担保ベース値が常に収益ベース値以上だった（PD係数1.8下でもEBITDAが
  収益ベース値を担保ベース値まで押し上げるには至っていない）。
- **develop/v2ではtierCapが実際にgrossLimitを縮小した行は0件**（§11-6・§15問い7で
  指摘した「tierCapがほぼ常に非拘束」という観察を、今回120行全数の集計で確認）。
  一方**pd_laborでは15件**、tierCapが実際にgrossLimitを縮小している
  ——PD係数1.8下でEBITDAが下押しされる一方、担保上限側の相対的な低下により
  tierCapとの大小関係が変わる状態が一部turn/一部社で発生していると推測される
  （原因の深掘りは§13の既存記述どおり本タスクのスコープ外）。
- **「余力はあったが既存債務控除で0になった」行が、develop/v2では76/120（63%）に
  対し、pd_laborでは120/120（100%）**——pd_laborは8四半期を通じて
  一貫してこの状態にあり、これが§13で報告した「pd_laborは一度も承認が回復しない」
  という観察の内訳である。
- `underwritingFrozen`はどちらのスタックでも0件——信用区分E・重大延滞・
  支払不能による申請ブロックは、標準初期条件のシミュレーション期間内では
  一度も発生しなかった（両スタック共通）。

---

## 14. 数値の価値分類（Exact live value / Derived exact value / Approximation）

本ドキュメント§11以降の数値は、原則として以下のいずれかに分類される
（§9以前の数値のうち、担保上限の内訳分解に依存していた候補2・3・5関連の数値は、
§11-3で「Approximation（担保内訳0仮定の近似）」だったことが判明し、
§11以降の値で置き換えられた）。

- **Exact live value**: `computeBorrowingCapacity`へ実際に渡された`borrowingCapacityInput`、
  またはその戻り値`borrowingCapacityResult`／`LoanRollForwardSnapshot`のフィールドを
  そのまま表示した値。§11-2〜§11-5・§11-7〜§11-10の基礎データ（担保上限・収益上限・
  tierCap・grossLimit・既存債務・ロールフォワードの各列）はこれに該当する。
- **Derived exact value**: Exact live valueのみを入力とする決定的な計算式
  （例: `computeBorrowingCapacity`をExact live valueへ別パラメータで再適用した候補2/5の結果、
  `w×tierCap`等）。§11-7〜§11-10の候補比較列はこれに該当する。
- **Approximation**: 真値を直接取得できず、別の値で代替・推測した値。
  §9-2・§9-3の担保上限内訳（原料在庫の輸送中/利用可能を0と仮定）、および
  §9-3の候補5表（equityを当期値で代用していた）がこれに該当し、
  §11の値で上書き・訂正済み。**現時点で本ドキュメントにApproximationとして
  残っている値はない**（§11以降は全てExact live valueまたはDerived exact value）。
  §9の該当箇所は削除せず、是正の経緯として残す（§9冒頭の記述どおりBEFORE/AFTER対比のため）。

**「完全一致」「実測」という語は、本ドキュメントでは同一時点の実データを指す場合にのみ使う。**
§9で一部「実測」と表記していた担保上限の内訳分解値は、正しくは近似値だった
——§11-3で訂正済み。

---

## 15. 最終判断のための問い（候補として提示。決定はしない）

1. **「通常融資承認ゼロ」の本質は何か**: 本番のバグ・初期fixtureの問題・与信仕様の問題・
   AI申請タイミングの問題のいずれか。 → 本診断の結論: **初期fixtureの問題**
   （標準初期条件の既存債務が、担保ベース上限に対して初期時点から重すぎる）が主因。
   与信計算式自体にバグはない（§9-2・§11-4で確認）。AI側の申請タイミングにも
   問題は見つかっていない（申請自体は毎ターン発生しており、拒否されているのは
   与信判断側）。
2. **初期債務削減だけで問題は解決するか**: §11-7の測定では、-50%で100%承認・
   緊急融資0件になるが、-20%〜-30%では緊急融資は残るものの承認率は改善する
   （41/81=51%〜46/76=61%）。**「解決」の基準（緊急融資を完全に0にするか、
   経営判断の余地を残すために一部残すか）はオーナー判断。**
3. **`earningsMultiple`変更が実際に効く状態はどこか**: EBITDAが黒字で蓄積した状態
   （turn3のような）でのみ効果大。初回四半期・EBITDA代理が薄い状態では効果ゼロ。
4. **四半期EBITDA×現行multiplierはゲーム内銀行審査モデルとして妥当か**:
   現行は保守的（年換算では年間EBITDA×0.5相当）。三宅さんの候補1提案
   （multiplier=8.0）は年換算で年間EBITDA×2相当となり、一般的な運転資金融資の
   感覚に近づく。**どちらを「妥当」とするかはゲームバランス上の判断。**
5. **年換算表現へ切り替える場合、どう水準を保つか**: §11-9のとおり、
   `四半期EBITDA×8` = `年換算EBITDA×2`。年換算表現へ切り替えるなら
   `multiplier`を4分の1に薄める必要がある（多重掛けをしない）。
6. **equity基準の追加（候補5）は本当に必要か**: 候補1・候補2でカバーできない
   turn1・turn5のような「EBITDA蓄積が薄い時期」にも効果があるのは候補5だけ
   （§11-10）。ただし信用区分の意味を薄める副作用リスクがある（§12）。
   **必要性は「EBITDA蓄積が薄い時期にも借入余力を持たせたいか」という
   ゲームデザイン判断に依存する。**
7. **creditTierCapは意図どおり機能しているか**: 現行の`grossLimit`式では
   tierCapは「絞る」側（外側`min`）としてほぼ常に非拘束（担保・収益の`max()`が
   tierCapを下回る状態が続いている）。§11-6の用語是正のとおり、
   tierCapが実際に効いている（縮めている）行は今回の120行中ほぼ皆無だった
   （正確な件数は次回作業で集計可能）。**「区分×資本を上限としてしか使わない」
   設計が意図どおりかは、オーナー確認事項。**
8. **通常融資が増えた場合の緊急融資・破綻・capex資金への波及**: §11-7の測定では、
   通常融資の承認拡大（債務削減）は緊急融資件数を単調に減らす（15→0件）。
   破綻頻度・capex資金への波及は本診断のスコープ外だが、緊急融資減少は
   間接的に財務健全性分類（`financialHealth`）の改善に寄与すると推測される
   （検証は次回作業）。
9. **develop/v2とpd_laborで結論は異なるか**: 構造的な結論（担保ベース値が支配的、
   EBITDA経由の直接効果はない）は同一。ただし**pd_laborは8四半期を通じて
   一度も回復しない**という定量的な違いがあり、担保上限側（原料/製品在庫の価値の
   積み上がり方）に根本原因があると推測される（未検証）。
10. **次に何をすべきか**: §16参照。

---

## 16. 推奨（3段階。今回は実装しない）

**最小修正案（minimal-fix）**: 標準初期条件の既存債務を-20%〜-30%程度に調整する
（候補1のみ、fixture修正のみ）。与信モデル自体には触れない。緊急融資は一部残るため
経営判断の重要性は保たれる。**実装コストが最小で、既存の与信モデルの意味を変えない。**

**バランス案（balanced）**: 候補1（fixture-20%〜-30%）＋候補2（earningsMultiple
2.0→4.0〜6.0程度）の併用。初期状態を是正しつつ、EBITDA黒字化という経営努力に
対する応答性も高める。信用区分の意味は変えない。

**経済的リアリズム重視案（more-economically-realistic）**: 候補1（fixture是正）＋
候補5（equity基準の追加、w=0.5程度）の併用。自己資本の厚い企業がより多く
借りられるようになり、現実の銀行与信（資産担保＋収益力＋自己資本の複合評価）に
近づく。ただし信用区分の意味が薄まるリスクがあるため、`creditTierEquityMultiple`
（tier別倍率）の再設計とセットで検討する必要がある——**本診断のスコープ外
（財務モデル仕様の変更判断はオーナー）。**

**いずれも今回は実装しない。三宅さんの決定を待つ。**

---

## 17. 次の作業の推奨

1. 三宅さんが上記3段階のいずれか（または独自案）を選定した後、実際の
   パラメータ・fixture変更を別コミットとして実装し、§11の各テストスイートを
   維持しながら変更後の挙動を再測定する。
2. ~~tierCapが実際に「絞っている」行が今回の120行中何件あるかを正確に集計する~~
   → §13-1で集計済み（develop/v2=0件、pd_labor=15件）。
3. ~~pd_laborが8四半期を通じて回復しない根本原因を測定のみで深掘りする~~
   → §18で対応済み（**訂正・2026-08-04**: 「担保上限自体がPD係数1.8下で
   既存債務と同じペースで縮小し続ける」ことはコードで確認された事実だが、
   これを引き起こしている経済的な根本原因（どの担保構成要素の変動が
   主導しているか）は§18時点では未確認のまま「原因」と言い切っていた。
   正しい言い方は — 債務残高が減っても、grossLimitは既存債務残高を
   下回ったまま追随し続けるため、追加借入余力が一度も回復しない。
   grossLimitが低い水準に留まる経済的な根本原因、および実際にどの
   担保構成要素が動いているかは、§20の構成要素別測定で確認する）。
4. R3/P6/P7等のSAI-3A標準初期条件候補の再選定は、本診断の結論
   （既存債務水準の是正が必要）を踏まえた上で、一貫した条件下で再実行する
   （Phase Aの未決事項のまま。三宅さんの7ステップ計画のstep6で対応予定、
   本タスクのスコープ外）。

**ブランチ状態のメモ（三宅さんのstep2判断材料・2026-08-04時点、
2026-08-04にgit方向の誤記を訂正）**:
`origin/develop/v2`のHEADは本ブランチの分岐点（`90d67bc`）から**動いていない**
（`git fetch`確認済み）。`90d67bc`は`feature/v2-financing-credit-diagnosis`の
祖先（merge-base）であり、本ブランチのHEADはそこからさらに進んでいる。
したがって正しい方向は逆で、**`origin/develop/v2`が本ブランチのHEADへ
クリーンにfast-forwardできる状態**にある（本ブランチが`develop/v2`へ
fast-forwardするのではない）。つまり統合作業としては、`origin/develop/v2`
というref自体を本ブランチのHEADの位置まで進める（fast-forward merge）
ことになる（コンフリクトなし。push権限が回復し次第、fast-forwardのみで
統合可能）。本メモはread-onlyの現状確認であり、マージ・rebase・pushは
一切実行していない。

---

## 18. pd_labor能力非回復サイクルの数値分析（三宅さん7ステップ計画step4・2026-08-04）

### 18-1 方法

`scripts/pdLaborCapacityCycleTrace.ts`（新規、develop/v2・pd_labor両方で
相対importのみで動作）を`/tmp/pd_labor`へ一時コピーし、3シード×BAL社
（標準baselineの5社は同一テンプレートのクローンで、同一seed・同一turnでは
完全に同一の数値になることを確認済み——他4社の代表としてBAL社を追う）で、
`ebitdaLikeQuarterlyUsd`（引受用EBITDA代理）・当期営業CF・通常/緊急融資実行額・
緊急融資の発動理由（不足額）・当期末ST+LT債務・翌turnの担保上限/収益上限/
tierCap/余力/承認額を、8四半期にわたって1つの表として追跡した。
実行後は`liquidityClose.ts`を`git checkout --`で復元、スクリプトファイルを
削除し、`git status`のcleanと`HEAD 5f1fa87`不変を確認済み（コミットしていない）。

### 18-2 代表ケース（seed=sai3a-grid-001・BAL社。他2シードもほぼ同型のパターン）

| turn | 引受用EBITDA代理 | 営業CF(当期) | 緊急融資実行(理由:不足額) | 当期末ST+LT債務 | 翌turn担保上限 | 翌turn余力 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 0 | 17,151,906 | - | 55,350,000 | 30,455,456 | 0 |
| 2 | −1,549,129 | −25,123,310 | - | 53,700,000 | 43,385,468 | 0 |
| 3 | 8,429,211 | −2,506,794 | - | 52,050,000 | 44,175,733 | 0 |
| 4 | 2,362,046 | 12,856,445 | - | 50,400,000 | 33,528,611 | 0 |
| 5 | −4,378,056 | −2,754,770 | 12,626,523（不足=26,784,000） | 37,376,523 | 32,578,165 | 0 |
| 6 | 3,358,197 | −1,176,479 | 2,806,635（不足=2,806,635） | 38,553,002 | 33,507,457 | 0 |
| 7 | −7,069,251 | 19,321,494 | - | 24,276,479 | 12,372,770 | 0 |
| 8 | −10,046,946 | −16,982,951 | 4,339,677（不足=5,062,497） | 28,616,156 | N/A（8Q終了） | N/A |

### 18-3 サイクルが切れない理由（数値による説明）

各turnの「翌turn余力 = max(0, 翌turn担保上限 − 当期末ST+LT債務)」を計算すると
（§13-1の集計どおり、pd_laborでは収益ベース値が担保ベース値を上回った行が
0件であるため、grossLimitはほぼ常に担保上限と一致する）:

| turn(N)→turn(N+1) | 当期末債務 | 翌turn担保上限 | 差（担保−債務） |
| --- | --- | --- | --- |
| 1→2 | 55,350,000 | 30,455,456 | **−24,894,544** |
| 2→3 | 53,700,000 | 43,385,468 | −10,314,532 |
| 3→4 | 52,050,000 | 44,175,733 | −7,874,267 |
| 4→5 | 50,400,000 | 33,528,611 | −16,871,389 |
| 5→6 | 37,376,523 | 32,578,165 | −4,798,358 |
| 6→7 | 38,553,002 | 33,507,457 | −5,045,545 |
| 7→8 | 24,276,479 | 12,372,770 | −11,903,709 |

**中心的な観測事実（訂正・2026-08-04: 以下は「原因」の確定ではなく、
コードで確認できた相関関係の記述。三宅さんの4つの仮説のうち(c)寄りの
「債務残高そのものが原因」は下記1で否定できるが、代わりに「担保上限自体が
債務と同じペースで縮小し続けるため、両者の差が一度もプラスに転じない」
という現象が観測される、という段階にとどまる。この現象を引き起こしている
経済的な根本原因、および実際にどの担保構成要素（売掛金・原料在庫・
完成品在庫のいずれ）が縮小を主導しているかは、§20の構成要素別測定
（実コードのCollateralInput内訳）で確認するまでは仮説である**:

1. **緊急融資の未返済が原因、という仮説は支持されない**——turn7の当期末債務は
   24,276,479までいったん大きく下がっている（turn6末の38,553,002から約38%減）。
   もし「緊急融資債務が返済されないこと」が主因なら、この時点で余力が
   プラスに転じてよいはずだが、実際には翌turn8の担保上限が**12,372,770**まで
   さらに大きく落ち込み、依然として届かない。**債務は実際に減っている
   （turn1の55.35Mからturn7には24.28Mまで半分以下）——「返済されていない」
   わけではない。**
2. **EBITDA代理が低いこと自体が直接の原因、という仮説も支持されない**
   ——§13-1・§13で確認済みのとおり、pd_laborでもgrossLimitの算定基準は
   一貫して担保ベース値であり（収益ベース値が選ばれた行は0件）、EBITDA代理の
   高低はgrossLimitに直接反映されない（`max(collateral, earnings)`で
   常にcollateralが選ばれ続けているため）。
3. **観測された相関（原因ではない）**: 担保上限（売掛金・原料在庫・完成品在庫の
   掛目後評価額の合算値）**自体が、債務残高とほぼ同じ大きさ・同じ方向で
   変動している**（turn1〜4は30〜44M、turn5〜6も32〜33Mで横ばい、
   turn7〜8にかけては12Mまで大きく縮小）。表の営業CF列（8turn中5turnが
   マイナス）と時期的には重なるが、**この一致から「営業CF悪化が担保資産を
   縮小させている」と因果関係を断定することは、本トレース（合算値
   `collateralBasedLimitUsd`のみを保持）からはできない**——これは仮説
   であり、§20の構成要素別測定（売掛金・原料在庫・完成品在庫それぞれの
   実測値）で確認するまでは未検証とする。緊急融資（turn5・6・8で発動）が
   既存債務の減少ペースに与える影響についても、同様に「観測された事実」
   （緊急融資実行額・当期末債務の推移）と「その先の解釈」を分けて扱う
   必要がある（turn5では通常融資が使えないため緊急融資12,626,523が
   全額を担っている、という点は観測事実）。
4. **したがって、初期債務削減（候補1）だけでは、担保上限自体の縮小
   トレンドまでは止められない**——債務を下げても、担保上限がそれ以上の
   ペースで縮んでいけば、差は再びマイナスに戻りうる。§19（Task C）の
   比較で、20%/30%/40%削減でどこまでこの縮小トレンドに対して耐性が
   生まれるかを実測する。

### 18-4 pd_labor: 未解決部分の明示（§20で構成要素別測定を実施）

担保上限（売掛金・原料在庫・完成品在庫の掛目後評価額）自体がPD係数1.8下で
どう決まっているか——具体的にどの構成要素（売掛金の回収サイクル、原料の
在庫水準、完成品の滞留）が縮小を主導しているか——は、§18時点では
未確認だった。**§20（2026-08-04追加）で`CollateralInput`の内訳
（`receivablesUsd`/`rawMaterialAvailableUsd`/`rawMaterialInTransitUsd`/
`finishedGoodsUsd`）は既存の観測フックで元々取得済みのデータであることが
判明し、要素分解そのものには追加の観測フックは不要だった（内訳の
「分析」がこれまで未実施だっただけ）。結果は§20を参照。**

---

## 19. 初期債務削減20%/30%/40%のpd_labor実測比較（三宅さん7ステップ計画step4 Task C）

### 19-0 A-6是正: B/S整合性の確認（2026-08-04追加）

三宅さんの新指示（A-6）は、本節の比較が「shortTermLoans/longTermLoansのみを
削減し、相殺勘定なしでB/Sが不整合になっている」おそれを指摘していた。
これを`scripts/pdLaborInitialDebtReductionBsCheck.ts`（新規）で実測確認した。

**結論: 実際には不整合ではなかった。** 4条件の初期財務状態を
`companyLab/standardAi/report/decomposeHarness.ts`の`buildUnifiedFinanceState`
（および本番の`finance/initialState.ts`の`buildInitialCompanyFinanceState`も同型）
経由で構築すると、`retainedEarnings`（利益剰余金）は
`totalAssets − totalLiabilities − capitalStock`という**残差（プラグ）**として
構造的に計算される（`finance/initialState.ts`のコード内コメント「開始時の
貸借一致の保証」に明記）。そのため`shortTermLoans`/`longTermLoansを削減すると、
`totalLiabilities`が減った分だけ`retainedEarnings`（＝純資産）が自動的に
同額増加し、資産合計は不変のまま「資産＝負債＋純資産」が常に厳密に成立する。

実測結果（BAL社、原料在庫を除く4項目ベース。差分列が4条件で完全に同一の
定数になっていることが、残差機構が機能している証拠）:

| 条件 | 資産合計(原料在庫除く) | 負債合計 | 資本金 | 利益剰余金(残差) | 純資産合計 | 差分(資産−(負債+純資産)) |
| --- | --- | --- | --- | --- | --- | --- |
| 0%（現行baseline） | 149,000,000 | 61,000,000 | 45,000,000 | 56,376,000 | 101,376,000 | −13,376,000（＝原料在庫分。4条件で一定） |
| -20% | 149,000,000 | 49,600,000 | 45,000,000 | 67,776,000 | 112,776,000 | −13,376,000（同上） |
| -30% | 149,000,000 | 43,900,000 | 45,000,000 | 73,476,000 | 118,476,000 | −13,376,000（同上） |
| -40% | 149,000,000 | 38,200,000 | 45,000,000 | 79,176,000 | 124,176,000 | −13,376,000（同上） |

原料在庫（4条件とも同一ロット構成のため一定額 13,376,000）を足せば、
4条件すべてで資産＝負債＋純資産が厳密に0差で成立する。

**したがって§19の以下の比較結果は、A-6が要求する「除外・reference onlyへの
降格」の対象ではない**——結果として最初からA-6の変種(b)
（「debtを減らしequityを同額増やす」）と数学的に同一の効果を持っていた
ことが確認できたため、**再計算は不要**であり、§19-2の数値はそのまま
有効である。ただし、この残差機構自体が「意図された相殺勘定」だったのか、
「たまたま構造的にそうなっていただけ」なのかは本診断のスコープでは
判断できない（三宅さんの判断事項として記録するのみ）。

### 19-1 方法

`scripts/pdLaborInitialDebtReductionCompare.ts`（新規）を`/tmp/pd_labor`へ
一時コピーし、develop/v2の候補1（§11-7）と全く同じ方法論——**静的スナップ
ショット再計算ではなく、初期fixtureのshortTermLoans/longTermLoansを実際に
削減し、8四半期をturnごとに実際に再シミュレーションする「本番再シミュ
レーション」**——をpd_labor上で実行した。3シード×8四半期×5社=120行、
4条件（0%/-20%/-30%/-40%）。実行後、pd_laborを§13と同じ手順で復元・
確認済み（コミットなし）。**（本番再シミュレーションであり、静的候補比較
ではない——§7参照。）**

### 19-1a A-8是正: 集計の独立性についての明示（2026-08-04追加）

標準baselineの5社（BAL/MASS/JPQ/VAP/CONSV）は`buildUnifiedFixturesFromTemplate`
経由で会社別overrideなしに同一テンプレートから複製されており、**同一seed・
同一turnでは5社全員が完全に同一の数値を出す**（実行して確認済み。§18の
コメントと同じ理由）。したがって下表の「120行」（3シード×8四半期×5社）は
**独立な観測点としては実質3シード×8四半期＝24点であり、それが5倍に
複製されているに過ぎない**。緊急融資総額・承認率などの集計値は、この
複製をそのまま合算・平均しているため、実質的な独立サンプル数は表面上の
1/5である点に注意が必要（本診断ではこれ以上の独立化はせず、複製込みの
生値をそのまま報告する。三宅さんが統計的な重み付けを必要とする場合は
別途要相談）。

### 19-2 結果

| 条件 | 短期+長期債務 | 初回承認turn | 緊急融資総額(3シード×5社複製込み) | 余力ゼロの行数 | 支払不能発生 | 債務超過発生 | 承認率 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0%（現行baseline） | 57,000,000 | **8Q以内に一度もなし** | 320,737,759 | 120/120（全行） | **true** | false | 0/105 = **0.0%** |
| -20% | 45,600,000 | 3 | 282,069,572 | 60/120 | false | false | 60/105 = **57.1%** |
| -30% | 39,900,000 | 3 | 247,817,251 | 70/120 | false | false | 50/105 = **47.6%** |
| -40% | 34,200,000 | 1 | 306,019,116 | 45/120 | false | false | 75/105 = **71.4%** |

### 19-3 読み取り（推奨は提示しない。数値のみ）

- **0%→-20%で最も大きな変化が起きる**: 支払不能(paymentDefault)が発生しなくなり、
  承認率が0%→57.1%へ跳ね上がる。**-20%が「支払不能を防ぐ」という観点での
  最初の分岐点**である。
- **-20%→-30%は非単調**——承認率がむしろ57.1%→47.6%へ**下がり**、
  余力ゼロの行数も60→70へ**増える**。緊急融資総額も282M→248Mへ下がるが、
  これは`standardBaseline.ts`のコード内コメントが既に警告している
  「moderate-pressure候補は財務パラメータの数百万USD単位の変化でも
  非単調に振れうる」という既知の性質と整合する。**-30%が-20%より
  常に良いとは限らない。**
- **-40%で再び大きく改善**（承認率71.4%、初回承認がturn1からに前倒し）。
  develop/v2側で-50%が「緊急融資を完全に0にする」という強い効果を
  示したのと似た挙動的方向性だが、pd_laborの-40%では緊急融資総額
  （306,019,116）はむしろ0%条件（320,737,759）に近い水準まで残っている
  ——**develop/v2の-50%ほど「解決しすぎ」にはなっていない**
  （緊急融資は残るが、通常融資の承認が大きく開く）。
- **支払不能(paymentDefault)は0%条件でのみ発生し、-20%以降は3条件とも
  発生しない**——支払不能を防ぐという意味では-20%で十分な可能性がある一方、
  「通常融資の承認が安定して機能する」という意味では-40%の方が
  承認率・初回承認timingの両方で明確に優れる。
- **どの水準を採用するかは、上記のどの指標を優先するか（支払不能回避か、
  承認率の高さか、緊急融資への依存度の低さか）に依存するトレードオフであり、
  本診断では特定の数値を推奨しない。三宅さんの判断事項とする。**

---

## 20. 担保構成要素別の内訳(A-3)と、本番の実測緊急融資不足額(A-4)（2026-08-04追加）

### 20-1 方法

`app/lib/v2/financing/liquidityClose.ts`の`LoanRollForwardSnapshot`へ、
observation-onlyの追加フィールド3つを新規に追加した（**既存フィールドは
一切変更していない。純追加のみ**。SNAP-1〜12は全てそのまま成立することを
再確認済み）:

- `shortfallBeforeEmergencyUsd`: 緊急融資判定に実際に使われる本番の変数
  `shortfallBeforeEmergency`そのもの（`closeQuarterWithFinancing`内、
  `max(0, fullAccruedInterest + scheduledPrincipalDue - max(0, availableForDebtServiceBeforeEmergency))`）。
- `cashBeforeEmergencyDecisionUsd`: 上式の`availableForDebtServiceBeforeEmergency`
  （Pass1後・利息元本現金支払前の現金残高）そのもの。
- `emergencyCapUsd`: 緊急融資枠の上限（絶対上限と担保比率上限の小さい方。
  緊急融資が発動しなかったturnは0）。

これらは新たな計算を追加したのではなく、**関数内で既に計算されていた
ローカル変数を観測用に露出しただけ**であり、本番の計算経路・戻り値には
一切影響しない（既存の`invokeObserverSafely`経由でのみ呼ばれる）。

担保構成要素（A-3）については、`UnderwritingSnapshot.borrowingCapacityInput.collateral`
（`receivablesUsd`/`rawMaterialAvailableUsd`/`rawMaterialInTransitUsd`/
`finishedGoodsUsd`）が既存の観測フック（§11-2）で元々捕捉済みだったため、
**追加の観測フックは不要だった**（§18-4時点の「追加フックが必要」という
記載は誤りであり、本節で訂正する）。

新規スクリプト`scripts/pdLaborComponentAndShortfallTrace.ts`で、develop/v2
自身（本番挙動のsanity確認）とpd_laborの両方で3シード×BAL社×8四半期を
実行した。実行手順は§18・§19と同じ（一時コピー→実行→`git checkout --`で
`liquidityClose.ts`を復元→スクリプト削除→`git status` clean・
`HEAD 5f1fa87`不変を確認。コミットなし）。

### 20-2 A-3: 担保構成要素の内訳（pd_labor、seed=sai3a-grid-001・BAL社）

| turn | 売掛金(素値) | 原料在庫使用可(素値) | 原料在庫輸送中(素値) | 完成品在庫(素値) | 掛目後:売掛金 | 掛目後:原料使用可 | 掛目後:原料輸送中 | 掛目後:完成品 | 担保上限合計 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 45,600,000 | 13,376,000 | 0 | 0 | 31,920,000 | 5,350,400 | 0 | 0 | 37,270,400 |
| 2 | 40,274,965 | 0 | 15,086,535 | 0 | 28,192,476 | 0 | 2,262,980 | 0 | 30,455,456 |
| 3 | 55,259,630 | 0 | 28,969,822 | 1,194,179 | 38,681,741 | 0 | 4,345,473 | 358,254 | 43,385,468 |
| 4 | 54,242,887 | 1,849,626 | 25,988,814 | 5,225,134 | 37,970,021 | 739,850 | 3,898,322 | 1,567,540 | 44,175,733 |
| 5 | 41,540,538 | 0 | 23,369,740 | 3,149,244 | 29,078,377 | 0 | 3,505,461 | 944,773 | 33,528,611 |
| 6 | 35,022,347 | 0 | 22,874,400 | 15,437,875 | 24,515,643 | 0 | 3,431,160 | 4,631,362 | 32,578,165 |
| 7 | 43,064,842 | 0 | 22,413,781 | 0 | 30,145,389 | 0 | 3,362,067 | 0 | 33,507,457 |
| 8 | 12,399,078 | 0 | 24,622,767 | 0 | 8,679,355 | 0 | 3,693,415 | 0 | 12,372,770 |

（他2シードもほぼ同型のパターン。掛目: 売掛金0.7・原料使用可0.4・原料輸送中0.15・完成品0.3、`parameters.ts`より）

**観測事実（コードで確認できる範囲）**:
1. **売掛金が一貫して最大の構成要素**であり、掛目後の担保上限のうち
   おおむね65〜85%を売掛金が占める（turn8のみ例外——後述）。
2. **原料在庫（使用可）はturn1以降ほぼ常に0**——turn4・6・7でわずかに
   非0になる程度で、担保上限への寄与は他の構成要素に比べ一貫して小さい。
3. **turn7→turn8の担保上限の急落（33.5M→12.4M）は、ほぼ全面的に売掛金の
   急落（43.1M→12.4M）で説明できる**（原料在庫輸送中は22.4M→24.6Mで
   むしろ微増、完成品在庫はturn7・8とも0）。これは§18-3で「担保上限自体が
   縮小し続ける」と観測されていた現象のうち、少なくともturn7→8の急落局面
   については**主に売掛金の減少が主導している、とコードから確認できる
   事実**である（§18-3の「仮説」を、この局面に限り観測事実へ格上げできる）。
4. **turn1→2の急落（37.3M→30.5M）は、原料在庫の分類が「使用可」から
   「輸送中」へ切り替わったことが大きい**（使用可13.4M→0、輸送中0→15.1M、
   掛目差0.4→0.15のため担保換算額が実質的に減少）。これは在庫の実量が
   減ったのではなく、**掛目の低い区分（輸送中）へ分類が移ったことによる
   担保換算額の減少**であり、§18-3で「事業規模自体の縮小」とだけ
   説明していた部分の少なくとも一部は、この掛目区分の切り替えで
   説明できる。
5. **turn5〜7の担保上限が32〜34Mで比較的横ばいなのは、複数構成要素の
   増減が相殺し合っている結果**であり、単一の要因には帰属できない
   （売掛金は35〜43Mの間で変動、原料輸送中・完成品在庫も個別に増減）。

**未確認のまま残る点（ハイポセシスとして明示）**: 売掛金・原料在庫自体が
なぜその水準で推移するか（受注量・生産量・回収サイクルとの関係）は、
本診断（財務モジュールのobservation-onlyフック）のスコープでは追えない
——販売・生産モジュール側のロジックを別途トレースする必要があり、
**これは本タスクの範囲を超えるため次回作業として申し送る**。

### 20-3 A-4: 本番の実測緊急融資不足額 vs 診断スクリプト旧近似（pd_labor・develop/v2）

**pd_labor（seed=sai3a-grid-001・BAL社）**:

| turn | Pass1後現金 | 本番shortfallBeforeEmergency | 緊急融資枠上限 | 緊急融資実行 | 旧近似(元本+利息-通常融資) | 差分(本番−旧近似) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 37,151,906 | 0 | 0 | 0 | 1,650,000 | −1,650,000 |
| 2 | 11,623,970 | 0 | 0 | 0 | 2,895,375 | −2,895,375 |
| 3 | 7,430,052 | 0 | 0 | 0 | 2,858,250 | −2,858,250 |
| 4 | 18,599,371 | 0 | 0 | 0 | 2,821,125 | −2,821,125 |
| 5 | 14,157,477 | 12,626,523 | 15,011,575 | 12,626,523 | 26,784,000 | **−14,157,477** |
| 6 | −19,844 | 2,806,635 | 14,573,503 | 2,806,635 | 2,806,635 | 0 |
| 7 | 20,564,241 | 0 | 0 | 0 | 15,519,270 | −15,519,270 |
| 8 | −11,937,980 | 5,062,497 | 4,339,677 | 4,339,677 | 5,062,497 | 0 |

**develop/v2（seed=sai3a-grid-001・BAL社、sanity確認）**:

| turn | Pass1後現金 | 本番shortfallBeforeEmergency | 緊急融資枠上限 | 緊急融資実行 | 旧近似(元本+利息-通常融資) | 差分(本番−旧近似) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 37,797,251 | 0 | 0 | 0 | 1,650,000 | −1,650,000 |
| 5 | 24,253,261 | 2,530,739 | 17,313,117 | 2,530,739 | 26,784,000 | −24,253,261 |
| 6 | 17,309,900 | 0 | 0 | 0 | 0 | 0 |

**観測事実**:
1. **旧近似（`scheduledPrincipal + scheduledInterest - normalDraw`）は、
   緊急融資が実際には不要な四半期（本番shortfall=0）でも正の値を示す**
   （turn1・2・3・4・7）——これは旧近似が`availableForDebtServiceBeforeEmergency`
   （Pass1後の実際の現金残高＝期首現金＋当期営業CF等を含む）を一切考慮せず、
   「通常融資実行額だけで足りるか」という粗い基準しか使っていないため。
   **旧近似を「緊急融資の理由（不足額）」として提示していた§18-2の表は、
   緊急融資が実際に発動しなかったturnについては実態と乖離した数値を
   示していたことになる**（§18-2の表自体は緊急融資が発動したturn5・6・8
   のみ「不足=」表記を出しており、発動しなかったturnは「-」表記のため
   実害はなかったが、次回同種のトレースを行う際は本節の実測値を使う）。
2. **緊急融資が実際に発動したturn（5・6・8）では、旧近似と本番実測が
   一致する場合（turn6・8: 差分0）と大きく乖離する場合（turn5:
   本番12.6M vs 旧近似26.8M）がある**——turn5の乖離は、Pass1後の現金
   （14.2M、期首現金・当期キャッシュフローを含む）が旧近似の前提
   （現金は通常融資実行額のみ）より大きかったため、本番の実際の不足額は
   旧近似よりかなり小さく済んでいたことを意味する。**§18-2で
   「不足=26,784,000」としていたturn5の数値は誤り（近似の限界）であり、
   正しい本番値は12,626,523（実行額と完全一致）である。訂正する。**

### 20-4 §18-2の数値訂正（A-4適用）

§18-2の表の「緊急融資実行(理由:不足額)」列は、turn5について
「12,626,523（不足=26,784,000）」と記載していたが、正しくは
「12,626,523（本番不足額=12,626,523、実行額と一致）」である。turn6・8は
元々旧近似と本番実測が一致していたため訂正不要（turn6: 2,806,635で一致、
turn8: 5,062,497で一致）。

---

## 21. A-10: pd_labor / 「48コミットスタック」の同一性のgit検証（2026-08-04追加）

`/tmp/pd_labor`ワークツリーで直接`git`コマンドを実行して確認した（推測ではない）:

- `git branch --show-current` → `feature/v2-product-labor-and-pd-mechanization`
- `git log --oneline -1` → `5f1fa87`（クリーン、本タスク開始時から不変）
- `git merge-base HEAD origin/develop/v2` → `083425a5253b2456f2e82b3902b17c79a178c4ed`
- `git rev-list --count origin/develop/v2..HEAD` → **48**

以上より、`/tmp/pd_labor`のブランチ`feature/v2-product-labor-and-pd-mechanization`
（HEAD `5f1fa87`）は、`origin/develop/v2`から正確に**48コミット**進んでいることを
git自身のカウントで確認した。これが本診断で一貫して「48コミットスタック」
「PD係数1.8」と呼んできた対象と同一であることを、推測ではなくgitコマンドの
出力で確定させた（A-10要求どおり）。develop/v2側は`origin/develop/v2`
（本ブランチの分岐点であり、§17で確認済みのとおりHEADは動いていない）を
比較対象としている。

---

## 22. Part A完了状況（2026-08-04更新）

三宅さんの指示（Part A-D）のうち、財務診断の是正（Part A）はA-1〜A-11の
全項目に対応した（§20・§23〜§26に詳細を追加）。要約:

- **A-1**: 完了（§1・§17・§18是正）。
- **A-2**: 完了（§23、時点ラベル付き完全中間値表。develop/v2・pd_labor
  双方でturn={1,3,5,6,7,8}分を作成。pd_laborのturn7で
  creditTierEquityCapが実際の選定基準になっている行を新規発見）。
- **A-3**: 完了（§20-2、担保構成要素別の内訳。turn7→8の急落を主に
  売掛金の急落で説明できることを観測事実として確認）。
- **A-4**: 完了（§20-3・§20-4、本番の実測shortfallBeforeEmergencyを
  捕捉、§18-2のturn5の数値を訂正）。
- **A-5**: 完了（§24、A-4の実測値を使った7点ロールフォワードの再構成。
  develop/v2・pd_labor双方で全turn完全一致を確認。pd_laborのturn6
  部分延滞・turn8全額延滞も実測値ベースで記録）。
- **A-6**: 完了（§19-0、B/S整合性を実測確認。retainedEarningsが
  残差として自動的に相殺するため、§19の既存結果は再計算不要と結論）。
- **A-7**: 部分完了（§26、静的比較と動的再シミュレーションの参照先を
  明示的に分類・注記した。ただしドキュメント全体の物理的な節の
  並べ替えまでは実施していない——次回、構成見直しの際に検討）。
- **A-8**: 完了（§25、拡充集計。develop/v2側で「5社完全複製」の前提に
  対する小さな例外——各seedにつき1社だけ緊急融資額がわずかに異なる
  ケース——を新規発見し、§25-1で訂正記載した。pd_laborでは完全複製が
  維持されていることも確認済み）。
- **A-9**: 完了（既存の§19-3の結論表現が既にA-9の要求と整合していることを
  確認、追加修正なし）。
- **A-10**: 完了（§21、gitコマンドで48コミット先行を確認）。
- **A-11**: 完了。`liquidityClose.ts`変更後もSNAP-1〜12は再実行・全件pass、
  `tsc --noEmit`エラーなし、`npm test`2150件全件pass、対象ファイル
  `eslint`エラーなし、`npm run build`はcompile・型検査まで成功
  （ページデータ収集時のRedis環境変数未設定エラーは本変更と無関係の
  既存インフラ課題）。production financingファイルへのdiffは引き続き0件。

**Part Aは以上で完了とする。** Part B（営業ハードキャップ調査）・
Part C（実装）・Part D（最終報告24項目）については、本ドキュメントの
続き（§27以降）およびPart B/C独自の成果物を参照。

---

## 23. A-2: 同一時点スナップショット完全中間値表（時点ラベル付き、2026-08-04追加）

`scripts/a2LabeledIntermediatesTable.ts`（新規）で、代表会社（BAL）・
seed=sai3a-grid-001・turn={1,3,5,6,7,8}について、以下の3時点区分を
明示した完全な中間値表を、develop/v2・pd_labor両方で取得した。

**時点区分の定義**（本番コードの実際の参照タイミングに基づく）:
- **[前期末インプット]** — `prevFinanceState`/`prevFinancingState`由来。担保
  4項目・EBITDA代理・総自己資本・既存債務残高・信用区分すべてがこの時点の値
  （銀行は「当期の意思決定確定時点で既に確定している情報」しか見ない設計）。
- **[当期・申請前]** — 上記インプットから`computeBorrowingCapacity`で算出した
  当期の引受枠（grossLimit・availableAdditionalCapacity等）。当期の実際の
  融資申請額とは独立に決まる。
- **[当期・申請後]** — 引受枠に対し当期の実際の申請額を適用した結果
  （承認/却下・approvedAmount・deniedAmount・reasons）。

### 23-1 develop/v2（seed=sai3a-grid-001・BAL社）

| turn | [前期末]担保上限計算前4項目合計 | [前期末]EBITDA代理 | [前期末]総自己資本 | [前期末]既存債務残高 | [前期末]信用区分 | [申請前]collateralBasedLimit | [申請前]earningsBasedLimit | [申請前]creditTierCap | [申請前]grossLimit | [申請前]selectedBasis | [申請前]availableAdditionalCapacity | [申請後]requestedAmount | [申請後]approvedAmount | [申請後]承認/却下 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 58,976,000 | 0 | 101,376,000 | 57,000,000 | B | 37,270,400 | 0 | 111,513,600 | 37,270,400 | collateralBased | 0 | 10,558,600 | 0 | 却下 |
| 3 | 81,990,303 | 12,425,418 | 107,691,743 | 53,700,000 | B | 44,707,642 | 24,850,836 | 118,460,917 | 44,707,642 | collateralBased | 0 | 15,361,893 | 0 | 却下 |
| 5 | 72,788,857 | 4,278,766 | 109,936,542 | 50,400,000 | A | 37,079,958 | 8,557,533 | 164,904,814 | 37,079,958 | collateralBased | 0 | 6,046,231 | 0 | 却下 |
| 6 | 75,335,216 | 5,078,236 | 111,511,931 | 27,280,739 | B | 38,602,092 | 10,156,472 | 122,663,124 | 38,602,092 | collateralBased | 11,321,353 | 28,768,977 | 11,321,353 | 承認(一部) |
| 7 | 73,142,558 | 5,273,341 | 113,624,120 | 36,952,092 | A | 37,447,329 | 10,546,681 | 170,436,180 | 37,447,329 | collateralBased | 495,237 | 13,500,149 | 495,237 | 承認(一部) |
| 8 | 75,122,733 | 5,776,794 | 115,998,951 | 33,266,590 | A | 39,875,844 | 11,553,588 | 173,998,427 | 39,875,844 | collateralBased | 6,609,254 | 15,664,518 | 6,609,254 | 承認(一部) |

### 23-2 pd_labor（48コミットスタック、seed=sai3a-grid-001・BAL社）

| turn | [前期末]EBITDA代理 | [前期末]総自己資本 | [前期末]既存債務残高 | [前期末]信用区分 | [申請前]collateralBasedLimit | [申請前]earningsBasedLimit | [申請前]creditTierCap | [申請前]grossLimit | [申請前]selectedBasis | [申請前]availableAdditionalCapacity | [申請後]requestedAmount | [申請後]approvedAmount | [申請後]承認/却下 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 0 | 101,376,000 | 57,000,000 | B | 37,270,400 | 0 | 111,513,600 | 37,270,400 | collateralBased | 0 | 10,558,600 | 0 | 却下 |
| 3 | 8,429,211 | 102,018,939 | 53,700,000 | B | 43,385,468 | 16,858,421 | 112,220,833 | 43,385,468 | collateralBased | 0 | 20,758,564 | 0 | 却下 |
| 5 | −4,378,056 | 93,673,554 | 50,400,000 | B | 33,528,611 | 0 | 103,040,910 | 33,528,611 | collateralBased | 0 | 13,102,160 | 0 | 却下 |
| 6 | 3,358,197 | 93,872,912 | 37,376,523 | B | 32,578,165 | 6,716,395 | 103,260,203 | 32,578,165 | collateralBased | 0 | 28,689,026 | 0 | 却下 |
| 7 | **−7,069,251** | 83,672,027 | 38,553,002 | **D** | 33,507,457 | 0 | **33,468,811** | **33,468,811** | **creditTierEquityCap** | 0 | 28,280,791 | 0 | 却下 |
| 8 | −10,046,946 | 70,407,334 | 24,276,479 | C | 12,372,770 | 0 | 52,805,501 | 12,372,770 | collateralBased | 0 | 24,141,975 | 0 | 却下 |

**新規の観測事実（A-2実施により初めて確認）**: pd_laborのturn7では、
**creditTierCapが実際に選定基準（selectedBasis）となっている**
（信用区分D・自己資本83.67M×0.4=33.47Mが担保上限33.51Mをわずかに下回る）。
develop/v2側では§13-1で「tierCapが実際に絞っている行は0件」と報告していたが、
**pd_laborでは少なくともこの1行（turn7・BAL社・seed001）でtierCapが真に
拘束条件になっている**ことが、A-2の完全中間値表で確認できた（§13-1の
「pd_labor=15件」という既存の集計と整合する——この15件のうちの1件が
本表で個別に確認できたことになる）。

---

## 24. A-5: 7点ロールフォワード再構成（A-4実測shortfallを反映、2026-08-04追加）

`scripts/a5RollForwardCorrected.ts`（新規）で、develop/v2・pd_labor両方の
BAL社・seed=sai3a-grid-001について、8四半期全turnの7点ロールフォワードを、
§20で追加した実測フィールド（`shortfallBeforeEmergencyUsd`等）を使って
再構成した。**両スタックとも全turnで(6)元本返済後 = (7)当期末債務が完全一致
（差分0）し、突合に成功した**（§11-5時点の突合は維持されたまま、旧来の
近似shortfallを実測値へ置き換えても整合性は崩れない）。

### 24-1 develop/v2（BAL社・seed=sai3a-grid-001、8turn全件）

| turn | (1)(2)前期末=当期開始 | (3)通常融資実行後 | (4)緊急融資実行後 | (5)返済前 | (6)返済後 | (7)当期末(本番実値) | 突合差分 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 57,000,000 | 57,000,000 | 57,000,000 | 57,000,000 | 55,350,000 | 55,350,000 | 0 |
| 2 | 55,350,000 | 55,350,000 | 55,350,000 | 55,350,000 | 53,700,000 | 53,700,000 | 0 |
| 3 | 53,700,000 | 53,700,000 | 53,700,000 | 53,700,000 | 52,050,000 | 52,050,000 | 0 |
| 4 | 52,050,000 | 52,050,000 | 52,050,000 | 52,050,000 | 50,400,000 | 50,400,000 | 0 |
| 5 | 50,400,000 | 50,400,000 | 52,930,739（緊急2,530,739） | 52,930,739 | 27,280,739 | 27,280,739 | 0 |
| 6 | 27,280,739 | 38,602,092（通常11,321,353） | 38,602,092 | 38,602,092 | 36,952,092 | 36,952,092 | 0 |
| 7 | 36,952,092 | 37,447,329（通常495,237） | 37,447,329 | 37,447,329 | 33,266,590 | 33,266,590 | 0 |
| 8 | 33,266,590 | 39,875,844（通常6,609,254） | 39,875,844 | 39,875,844 | 38,225,844 | 38,225,844 | 0 |

turn5の緊急融資: 実測shortfallBeforeEmergency=2,530,739（緊急融資枠上限
17,313,117、Pass1後現金24,253,261）。**旧近似（§18-2形式なら「不足=
26,784,000」）とは大きく異なる**——本番は実測どおり2,530,739のみが
不足額であり、緊急融資実行額と完全一致する。

### 24-2 pd_labor（48コミットスタック・BAL社・seed=sai3a-grid-001、8turn全件）

| turn | (1)(2)前期末=当期開始 | (3)通常融資実行後 | (4)緊急融資実行後 | (5)返済前 | (6)返済後 | (7)当期末(本番実値) | 突合差分 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 57,000,000 | 57,000,000 | 57,000,000 | 57,000,000 | 55,350,000 | 55,350,000 | 0 |
| 2 | 55,350,000 | 55,350,000 | 55,350,000 | 55,350,000 | 53,700,000 | 53,700,000 | 0 |
| 3 | 53,700,000 | 53,700,000 | 53,700,000 | 53,700,000 | 52,050,000 | 52,050,000 | 0 |
| 4 | 52,050,000 | 52,050,000 | 52,050,000 | 52,050,000 | 50,400,000 | 50,400,000 | 0 |
| 5 | 50,400,000 | 50,400,000 | 63,026,523（緊急12,626,523） | 63,026,523 | 37,376,523 | 37,376,523 | 0 |
| 6 | 37,376,523 | 37,376,523 | 40,183,158（緊急2,806,635） | 40,183,158 | 38,553,002（**一部延滞**） | 38,553,002 | 0 |
| 7 | 38,553,002 | 38,553,002 | 38,553,002 | 38,553,002 | 24,276,479 | 24,276,479 | 0 |
| 8 | 24,276,479 | 24,276,479 | 28,616,156（緊急4,339,677） | 28,616,156 | 28,616,156（**元本・利息とも支払0＝全額延滞**） | 28,616,156 | 0 |

**新規の観測事実**: turn6は`scheduledPrincipalDue=1,650,000`に対し
`principalPaidCash=1,630,156`（19,844不足）——**部分的な元本延滞**が
実際に発生している。turn8は`scheduledPrincipalDue=4,456,635`・
`scheduledInterestDue=605,862`に対し、`principalPaidCash=0`・
`interestPaidCash=0`——**元利金とも全額延滞**（この期は
`emergencyCapUsd=4,339,677 < shortfallBeforeEmergency=5,062,497`で
緊急融資枠自体が不足額を下回り、残り722,820が未解消のまま残ったことが
実測値から確認できる）。この延滞にもかかわらず(6)=(7)で厳密に一致して
おり、7点ロールフォワードの整合性は延滞ケースでも崩れていない。

---

## 25. A-8: 緊急融資 拡充集計（2026-08-04追加）

`scripts/a8EmergencyLoanAggregation.ts`（新規）で、develop/v2・pd_labor
双方について、grand total・独立サンプル換算・turn別/シード別/会社別内訳・
最大/最小/中央値・緊急融資ゼロのシード数を集計した。

### 25-1 develop/v2

| 指標 | 値 |
| --- | --- |
| grand total（5社複製込み、3シード） | 49,746,553 |
| イベント数（5社複製込み） | 15 / 120行 |
| 独立サンプル換算（BAL社のみ、3シード×8turn=24行） | 9,969,120（3イベント） |
| turn別発動 | turn5のみ（3シードとも） |
| シード別（独立サンプル） | 001: 2,530,739 / 002: 3,566,496〜3,156,927(MASS例外) / 003: 3,871,885〜4,110,657(MASS例外) |
| 最大/最小/中央値（独立サンプル） | 3,871,885 / 2,530,739 / 3,566,496 |
| 緊急融資ゼロのシード数 | 0/3 |

**A-8実施中に判明した是正事項（5社完全複製の前提の一部修正）**: develop/v2
では、**各seedにつき1社だけ、他4社とわずかに異なる緊急融資実行額を示す
ケースが存在する**（例: seed002はMASSのみ3,156,927、他4社は3,566,496。
seed003もMASSのみ4,110,657、他4社は3,871,885。seed001はBALのみ
2,530,739、他4社は2,548,677）。**pd_laborでは3シードとも5社が完全に
一致しており（会社別内訳がすべて同一値）、この例外はdevelop/v2側でのみ
観測された。** 差異は数%程度と小さく、本診断の主対象であるBAL社基準の
分析（§18〜§24）の妥当性そのものには影響しない（pd_labor側は完全一致の
ため無関係、develop/v2側もBAL社を代表として一貫して使用しているため）。
ただし、これまで§18等で「5社は同一seed・同一turnで完全に同一の数値になる」
と断定していた記述は、**develop/v2に関しては「ほぼ常に同一だが、稀に1社が
処理順序等の理由でわずかに異なる例外がある」へ訂正する**。この例外の
根本原因（会社の処理順序に依存する共有資源の配分ロジック等が疑われるが
未検証）は、本ラウンドのスコープでは深掘りしていない（次回申し送り）。

### 25-2 pd_labor（48コミットスタック）

| 指標 | 値 |
| --- | --- |
| grand total（5社複製込み、3シード） | 320,737,759 |
| イベント数（5社複製込み） | 45 / 120行 |
| 独立サンプル換算（BAL社のみ） | 64,147,552（9イベント）＝grand total÷5と完全一致 |
| turn別発動 | turn5(42,237,009)・turn6(8,644,112)・turn8(13,266,431) |
| シード別（独立サンプル） | 001: 19,772,836 / 002: 22,408,041 / 003: 21,966,675 |
| 最大/最小/中央値（独立サンプル） | 14,941,683 / 2,806,635 / 4,391,398 |
| 緊急融資ゼロのシード数 | 0/3 |
| 会社別内訳 | 5社とも完全一致（64,147,552）— develop/v2で見られた例外はpd_laborでは発生していない |

支払不能(paymentDefault)発生イベント数・債務超過(negativeEquity)発生
イベント数は、既存の§19-2（0%条件でtrue/false）で会社横断の有無のみ
記録済み。turn単位でのイベント発生数の内訳は本ラウンドでは未集計
（次回申し送り）。

---

## 26. A-7: 静的比較と動的再シミュレーションの明示分離（2026-08-04追加）

三宅さんの指示は「動的再シミュレーションの承認率変化を『財務条件単独の
効果』と誤読させない」ことを求めている。本ドキュメント内の該当箇所を
以下のように分類・明示する（今回のラウンドでは節の物理的な並べ替えは
行わず、この一覧で参照先を明示することで代替する。次回、ドキュメント
全体の構成見直しの際に物理的な再配置を検討する）:

**【静的スナップショット比較】**（1つの引受スナップショットを固定し、
財務条件のみを変えて再計算。会社の意思決定・生産・販売は一切変わらない）:
- §11-7候補1（初期債務・fixtureの調整、現金と分離した単独変化）
- §11-8候補2（`earningsMultiple`変更）
- §11-9候補3（四半期EBITDA vs 年間換算EBITDA）
- §11-10候補5（`grossLimit`の構造変更）
- §6-2〜6-4（旧版・参考）

**【動的8四半期再シミュレーション】**（初期条件を変えた上で、Standard AIの
意思決定・生産・販売・緊急融資・信用区分まですべて本番ロジックで
再実行し直す。財務条件の変化に対し、会社の行動自体が内生的に変わる）:
- §9-6（48コミットスタックでの再測定）
- §18（pd_labor能力非回復サイクルの数値分析）
- §19（初期債務削減20%/30%/40%比較）
- §20（担保構成要素・実測緊急融資不足額）
- §24（A-5ロールフォワード再構成）
- §25（A-8拡充集計）

**上記の動的再シミュレーション群（特に§19の承認率57.1%/47.6%/71.4%等の
数値）は、「初期debt水準を変えた場合に会社の行動全体がどう変わるか」の
結果であり、「融資条件（担保掛目・tierCap倍率等）だけを変えた場合の
単独効果」ではない。** 動的再シミュレーションでは、初期debt水準の変化が
→ 各turnの融資判定 → 承認/却下 → 緊急融資の要否 → 生産・販売の制約
→ 翌turnの財務実績（EBITDA・自己資本・担保） → 翌turnの融資判定 …
という循環を通じて、Standard AIの行動自体を内生的に変化させ続ける。
したがって§19の承認率の差は、静的比較（§11-7〜11-10）の「同一時点での
即時的な承認額の差」とは性質の異なる指標であり、両者を同列に「効果の
大きさ」として比較すべきではない。
