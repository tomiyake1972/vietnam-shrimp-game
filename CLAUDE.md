# CLAUDE.md — ShrimpX V2 作業ルール

## 1. 最終報告の出力形式（最重要・毎回必ず守る）

**最終報告は必ず「1回でコピペできる単一のコードブロック」で出力する。**

- 章ごとに分けて地の文で書かない。番号付きの報告項目はすべて**1つの fenced code block の中**に入れる。
- ブロック内はプレーンなテキスト／見出し／箇条書きで書き、**ブロック内で三重バックティックを使わない**
  （途中でブロックが切れてコピペできなくなるため）。
- ブロックの外に置いてよいのは、前置きの1〜2文と、必要なら最後の質問だけ。
- 指示に「N項目を報告」とある場合は、その番号と順序をそのまま維持する。
- 数値は実測値を書く。推測値・概算しか無い場合は「未実測」と明示する。

理由: 報告はそのまま ChatGPT #05 へ貼り付けて共有されるため、
分割されていると毎回手作業でつなぎ直す必要が生じる。

## 2. ブランチ運用

- 開発は指示されたブランチ（例 `claude/...`）で行い、そのブランチへ push する。
- CURRENT INTEGRATION は `integration/v2-current-20260818`。古い feature ブランチを単独の基準にしない。
- Pull Request は**明示的に依頼されたときだけ**作成する。

## 3. 検証コマンド

- 型チェック: `npx tsc --noEmit -p .`
- Lint: `npx eslint <path>`
- テスト: `npm test`（node:test / tsx。全体で約2〜3分）
- 単体: `npx tsx --test <path/to/*.test.ts>`

**既知の環境事象**: `npm run build` は、TypeScript コンパイル成功後に
V1既存route `/api/game/[gameCode]/admin/clone` で `STAGING_KV_REST_API_URL` 未設定エラーにより停止する。
これは開発日誌に記録済みの既存事象であり、変更内容とは無関係。報告時はその旨を明記する。

## 4. 実装方針

- **推測で修正しない。** まず実際の挙動を再現・実測してから直す。
- 仕様・エンジン式の変更禁止指示（変更禁止リスト）は厳守する。逸脱が必要と判断した場合は、
  勝手に変えず **Stop Condition として報告**する。
- 数値パラメータを新設するときは、既定値を現行と完全同一の挙動になる値にしてから有効化する。
- 存在しないバージョン番号・出典・数値を作らない。取得できない場合は「取得不能」と書く。
